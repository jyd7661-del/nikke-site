# 열려 있는 작업 · 판단 대기

> **이 파일은 "지금 열린 것"만 둔다**(2026-09-14 정리 — 4,034줄이던 것을 옮겼다).
> 경위·실측·함정은 **`docs/log/YYYY-MM.md`**(새것이 위)에 있고, 각 항목 끝의 `→ 09 절제목`이 그 위치다.
> 코드 주석의 `docs/open-items.md` 참조도 log에서 절 제목으로 찾으면 된다.
>
> **쓰는 법:** 작업을 하면 경위는 log 맨 위에 절로 쓰고, 여기엔 표 한 줄만 고치거나 지운다.
> 닫힌 항목을 여기 쌓지 않는다.

## 현재 기준선 (2026-09-14)

| 지표 | 값 | 뜻 |
|---|---|---|
| `verify` | 20종 통과 | `CLAUDE.md` 목록. 2026-09-18에 `testEngineDeterminism` 추가 |
| **사이트 추천 ↔ 내 판정 일치율**(`testJudgmentMatch`) | **22/38 = 57.9%** (비슷 2건 제외, 2026-09-20) | 유저 지시의 "같아질 때까지"를 재는 수치. 목표 100%. ⚠️ 이 수치만 보고 채택 금지 — 이전 엔진 대비 판정도 함께(스크립트 머리 주석) |
| **랭커 조합 백분위**(메타 풀, `testRankerTeams`) | **중앙 70%** · 솔로 68 · 타워 68 · 캠페인 92 · PvP 70.5 | 50% = 무작위. 214팀 중 62팀이 50% 미만 |
| 보스 티어 순위상관 ρ | 0.591 | 캐릭터 1명 단위 |
| 발동 조건 미분류 | 179절(27%) | 래칫 |
| AI 조합 실험(12문항, 메타 풀 백분위) | 스킬 원문만: Haiku 39.8 · Sonnet 59.2% → **+채용률 등급: Haiku 69.4 · Sonnet 80.3%** | 같은 잣대. 등록 1위 68.7%. 소넷+등급이 엔진(70%)을 넘었다 |
| **얇은 로스터 A/B(씨앗 1·2 = 40건, 판정=클로드)** | **AI 21 · 엔진 9** · 동일 10 (씨앗별 12:3 · 9:6) | 엔진 쪽은 폴백 36건. AI 승 = 엔진이 버프 대상 없는 캐릭터를 넣음. 엔진 승 = PvP 게이지 속도·조건부 시너지 짝. AI 답 자체의 분산 8/20 |
| 도감 · sitemap | 200페이지 · 205 URL | 홈 선택 목록은 SSR 171 + SR 20(토글) — 2026-09-15 |
| 방문 | 하루 평균 12.3명(08-27~09-13) · AI 유료 호출 하루 약 2회 | 호출 IP가 거의 1개 → 09 캐시 적중률 |

## 성장 계획 3축 — 단일 출처

| 축 | 상태 |
|---|---|
| **성장** | Phase 0(계측)·1(도감) 완료. 병목이 색인 면적 → **순위**로 옮겨갔다(08-30 노출 1,040 · 클릭 13 · 10.9위). `/guide` 계산형 분석 글 3편(09-03). **09-19: 검색 하락 — 구글이 렌더링한 페이지가 영어였다(봇에게 언어 자동 선택이 걸림). 봇 제외로 고쳤다. 09-20 구글봇 렌더링 재확인 — 홈·도감 모두 `lang=ko`·한국어 제목.** **09-20: 언어별 주소 `/en`·`/ja` + hreflang을 main에 병합(유저 승인). sitemap 205 → 613 URL** → 08 실적 스냅샷, 09 영어·일본어 검색 · 09 언어별 주소 |
| **수익** | 애드센스 **3차 반려(2026-09-03, "가치가 별로 없는 콘텐츠")** — 소유권 ✅라 도메인 가설 종료. **2026-09-04 유저: 판단 기준은 애드센스가 아니라 "유저가 유용하다고 느끼는가".** 재심사 요청은 안 했다 |
| **자동화** | 주간 점검 `weeklyCheck.mjs`(이 PC 작업 스케줄러) 가동 · 자체 방문 계측 가동(날짜 경계 UTC — **고치지 않기로 결정**). 주간 성장·수익 리포트 자동화는 승인됐고 설계 전 |

## 🙋 사람이 해야 하는 것 — 클로드가 못 한다

| 항목 | 왜 | → 경위 |
|---|---|---|
| **Supabase SQL 편집기에서 `supabase/ai_teams_migration.sql` 실행 → Vercel `AI_TEAMS_MODE=shadow` → 재배포** | AI 조합 구성 셰도우 시작. 순서를 지킬 것(파일 안 주석). 안 돌리면 라우트가 조용히 건너뛴다 | 09 AI 조합 구성 운영 코드 투입 |
| `nikke-site/.env.local`에 `ANTHROPIC_API_KEY` | 서브에이전트 실험을 **API로 대조**(같은 12문항 `--live`, 수백 원). 서브에이전트가 정답지를 안 봤는지 기록으로 확인 못 했다. 운영 전환 때도 필요 | 09 AI 조합 실험 1차 |
| enikk `champion-arena` → Teams 탭이 보이는지 | PvP 데이터(`topTeams·pairs·trios·quads`)가 **2026-08-21 이후 낡음**, 주간 점검이 매주 경고. 우리 브라우저에선 본문 0자였다 | 09 enikk 데이터 갱신 |
| Search Console에 **www 주소**가 뜨는지 | 08-30에 www 3건 노출. 308은 걸려 있는데 색인 신호가 갈렸다. 미처리 | 08 실적 스냅샷 |
| **Search Console에 sitemap 다시 제출 + `/en`·`/ja` 대표 페이지 색인 요청** | 09-20 언어별 주소 배포로 sitemap이 205 → **613 URL**이 됐다. 구글이 새 주소를 알아야 영어·일본어 검색이 시작된다. 한국어 회복은 09-20 구글봇 렌더링으로 확인했다(수치는 Search Console에서 볼 것) | 09 언어별 주소 |

## 🔧 시스템 보완 — 사이트 추천을 내 판정에 맞춰 간다 (2026-09-18 유저 지시)

지표는 `node scripts/testJudgmentMatch.mjs`. 고칠 때마다 가드를 함께 본다 —
`testRankerTeams`(메타 풀 중앙 70.5%) · `testRealTeams`(0건). 깎이면 그 변경은 되돌린다.

| 순서 | 항목 | 상태 |
|---|---|---|
| 1 | 엔진 결정성(같은 로스터면 순서와 무관하게 같은 답) | ✅ 2026-09-18. 역테스트 17/40 → 0/40 |
| 2 | 표본 재생성 + 재판정 11건 | ✅ 2026-09-18. 40건 온전, 50.0% |
| 3 | D1(조건부 버프가 받을 공격형 없이 빈다) 동점 처리 | ✅ 2026-09-18 **2판**. 1판은 이전 엔진 대비 4:4로 버림, 2판 3:1:2 채택. 남은 퇴행 1건(버프 크기를 안 봄) |
| 4 | 불일치 18건 원인 분류 | ✅ 2026-09-18. D2 PvP 버스트 5(데이터 없음) · D1 4 · D3 버퍼가 딜러를 못 키움 4 · D4 딜러 수 2 · D6 모드 적합 2 · D5 유연 버스트 1 |
| 5 | D3 — 아군 버프가 전부 비는 멤버 = 낭비 칸 | ✅ 2026-09-20. **동점 처리가 아니라 점수**(엔진 쪽 티어가 더 높았다). 53.8% → 57.9%, 이전 엔진 대비 나아짐 4·나빠짐 0 |
| 6 | 남은 D3 3건 · D5 | 열림 — 남은 D3은 "빈 버프"가 아니라 "버프가 약하다"라 잣대가 없다. D5는 유연 버스트 배치 |
| — | 판정 파일이 `probe-data/`(gitignore)에만 있다 | 판단 대기 — 이 지표의 정답지라 잃으면 수치가 사라진다. 추적 대상으로 옮길지 |

## ⚖️ 판단 대기 — 유저 결정

| 항목 | 선택지·쟁점 | → 경위 |
|---|---|---|
| **AI 조합 구성 — 셰도우 실측 대기** | 코드 투입 완료(기본 off). 사람 손 3단계 뒤 `ai_team_shadow`에서 위반률·엔진과 차이·지연을 보고 `on` 결정. 그다음: 초보 로스터 씨앗 3, 응답 분산 관측 | 09 AI 조합 구성 운영 코드 투입 |
| AI 조합 다음 실험 | 2차(채용률 등급)로 소넷 80%·완전일치 4건. 남은 실패 = 솔로레이드 수냉·전격 코어 교체·PvP. 다음 = ① 3차 `soloRaidByElement` 추가 변형 ② 키 생기면 `--live` 대조 | 09 AI 조합 실험 2차 · 1차 |
| 🔴 **버스트 3 단독 편성에 경고가 없다** | 버스트 3은 전원 쿨 40초인데 혼자 맡으면 엔진이 말이 없다. 추천 결과의 20~35% | `docs/engine.md` 4-3-c |
| 추천 화면 개선 3종 | ① 폴백으로 나왔음을 밝히기 ② "무엇을 얻으면 조합이 열리는가"를 전면에 ③ 원맨아미 3개를 ② 후보에서 빼기(①②가 먼저) | 09 2026-09-02 비교기 "남은 것 — 판단 대기" |
| 5인이 아닌 아키타입의 점수 | 1인 등록에도 완전일치 14점. 게시판 배지·추천 순위를 같이 움직여 실측 없이 안 건드림 | 같은 곳 |
| ①이 후보를 하나만 내놓음 | 사용률 1위만 ②와 겨룬다. 빈도 0.2~0.4%. 선정 의미가 바뀌는 변경 | 09 2026-09-02 오늘치 500회 |
| 도감 빈 절 102페이지 | 고유 집계가 안 붙는 캐릭터. "도감은 새 판정을 만들지 않는다"와 충돌 가능 | 09 애드센스 3차 반려 |
| 아르카나 토템 등록 | 보류 유지 — 근거가 공략 권장 조합뿐. `totemRole`은 언제나 B등급 | 09 2026-09-01 |
| 네온 : 비전 아이 PvP (우리 S vs prydwen SS) | 2곳 이상 대조 규칙. 판정 뒤 `tierJudgments.json`에 기록 | 주간 보고서 2026-09-14 |
| 맥스웰 : 오디너리 미케닉 PvP | 보류 — PvP 등급을 주는 곳이 prydwen뿐 | 08 prydwen 티어 불일치 8건 |
| CMP 동의 배너 · 개인정보처리방침 번역 | 법적 판단. 애드센스 통과 시점부터 EEA 수익 0 | 08 현재 열려 있는 항목 |

## 🧮 시뮬레이터 백로그 — 우선순위는 랭커 백분위로 고른다

| 항목 | 막힌 이유 |
|---|---|
| 방어형(목단 등)의 가치 | 도발·생존인데 적 공격력 데이터가 없다. 지금은 점수가 아니라 `bossDefenseNote` 정보 문장으로만 |
| 모드별 적 수 | PvP 5는 게임 형식이라 근거가 있다. `scoreComposition`이 모드를 안 받는다 |
| 무기별 연사속도 | 자체딜의 81~100%가 평타. Fandom에 장탄·재장전·1발당은 있고 **연사가 없다** |
| 지속 표기 없는 12절 · 차지 속도 18절 | 이름 붙은 상태 의존 / `▼300%`로 공식 미확정 |
| 발동 조건 미분류 179절 | 래칫 |
| 원소 상성 · CC · 버스트 순환 · 관통/다단히트 · 락온 · 풀차지 · 요격 가능 투사체 | 모델에 축이 없다 |
| 무작위 로스터 탐침 — 애장품 유무·보스 속성별·타워 기업별로 대보기 | 아직 안 해봄 |

## 📭 데이터 공백 — 출처가 막혀 채우지 않은 것 (지어내면 B등급)

| 항목 | 상태 |
|---|---|
| 스킬 `desc_ja` 12 · `desc_kr` 9 (래칫) | 아이기스·유키코 일본어는 **game8이 두 페이지 본문을 뒤바꿔** 막힘(`game8PageMap`은 맞다, 고치지 말 것). 드레이크 : 그레이트 빌런 한국어는 나무위키 자리표시자 |
| 솔로레이드 보스명 번역 5 · `squad` 일본어 1(`WILLE`) | 1차 출처 필요(`testDataI18n` 래칫) |
| 마코토 · 유키코 `squad` | 영문 표기 출처 없음 |
| 부대 소속 2건(`privaty-unkind-maid`·`arcana`) · `WILLE` 일본어 | 출처끼리 다르다. 게임 내 표기 확인 필요 |
| 마르차나 : 마린 스터디 투자 노트 · 맥스웰 : 오디너리 미케닉 투자 정보 | 앞은 스킬과 안 맞음(확신도 낮음), 뒤는 비워둠 |
| 모호어 3건 `명함`·`풍스카`·`클루드` | 용어집 확정 표기 필요 |

## 🗂️ 급하지 않음

- 피드백 라우트가 `tower`를 안 넘긴다 — 타워별 만족도를 보려면 마이그레이션 필요
- AI 설명이 미보유를 긍정으로 뒤집는 문장 — 출력 검사 후보. 재현 빈도부터
- 런타임 `@anthropic-ai/sdk` 0.32.1 — AI 조합 전환 때 올린다(실험은 dev 별칭 0.125.0)
- AI 조합 전환 시 전역 상한을 **원 단위로** 다시 정할 것(단가 13~66원이면 1,000회 = 1.3만~6.6만 원/일)
- 주간 점검의 prydwen 티어 추출이 "처음 나오는 값" 방식 — 한 페이지에 다른 캐릭터 카드가 박히면 틀린다(기존 188명은 무해)
- `SUPABASE_ACCESS_TOKEN`(선택) · 옛 `div_*` 테이블 · AI 테이블 anon 잔여 권한
- `app/layout.js` 메타데이터 i18n · `app/combos` 모드 판별 정규식이 한국어만 · 테트라 타워 월요일 개방 여부
- Phase 3 커뮤니티 시드(주간 조사 → 글 초안, 게시는 사람이)
