# 니케 조합 추천 사이트 — 인수인계 문서

> 새 Cowork 프로젝트/세션에서 이 작업을 이어받기 위한 문서입니다.
> **작성 시점: 2026-08-08.** 이 문서는 "지금까지 무엇을, 왜 그렇게 했는가"에 초점을 둡니다.
> 코드가 어떻게 생겼는지는 저장소를 읽으면 되지만, **왜 그런 선택을 했는지와 어떤 함정을
> 밟아봤는지**는 여기에만 있습니다. 같은 실수를 반복하지 않으려면 먼저 읽어주세요.

---

## 0. 새 세션에서 가장 먼저 할 일

```bash
cd <클론>/nikke-site
node scripts/checkData.mjs     # 기준선: ERROR 0 / WARN 0
node scripts/findTotems.mjs    # 기준선: 1군 0명
```

두 스크립트가 이 프로젝트의 안전망입니다. 뭔가 고친 뒤에는 반드시 다시 돌리세요.
`checkData`가 ERROR를 내면 그 변경은 되돌리는 것이 원칙입니다.

---

## 1. 프로젝트 개요

모바일 게임 **승리의 여신: 니케**의 조합 추천 사이트입니다.
보유 캐릭터를 선택하면 캠페인/보스전/PvP에 맞는 5인 조합을 추천합니다.

| 항목 | 값 |
|---|---|
| GitHub | `jyd7661-del/nikke-site` |
| 배포 | https://nikke-site.vercel.app (Vercel, 프로젝트 루트는 `nikke-site/` 하위 디렉터리) |
| 로컬 클론 | `C:\Users\정연도\Desktop\nikke-site-git\` |
| 프레임워크 | Next.js 14 App Router |
| DB | Supabase (프로젝트 `yttfwroeyplwrchyitud`) |
| AI | Anthropic API (`claude-haiku-4-5`) — **설명 문장 생성 전용** |

### ⚠️ 바탕화면에 비슷한 폴더가 두 개 있습니다

| 폴더 | 정체 |
|---|---|
| `Desktop\nikke-site-git\` | **실제 작업하는 git 클론.** 여기서 작업하세요 |
| `Desktop\nikke-site\` | 2026-07-26에 멈춘 옛 사본. git 연결 없음. **건드리지 마세요** |

한 번 혼동해서 파일을 못 찾은 적이 있습니다. 정리하거나 이름을 바꿔두면 좋습니다.

---

## 2. 핵심 설계 원칙 — 이걸 먼저 이해해야 합니다

### 2-1. AI는 조합을 만들지 않습니다

**조합 구성·점수·근거는 100% `lib/synergyEngine.js`가 결정합니다.**
AI는 이미 확정된 5명을 자연스러운 한 문단으로 설명하는 일만 합니다.

이건 여러 차례 시행착오 끝에 확정된 구조입니다. 예전에는 AI에게 자유 구성을 맡겼는데,
"왜 이 조합이 나왔는지" 설명이 계속 부실했고 사용자가 반복해서 지적했습니다.
**절대 AI에게 조합 구성을 돌려주지 마세요.**

이 구조 덕분에 엔진이 **완전히 결정적(deterministic)** 이고, 그래서 캐싱이 가능합니다(§6).

### 2-2. 근거 없는 숫자를 만들지 않습니다

이 프로젝트에서 반복적으로 확인된 원칙입니다. 여러 번 "가중치를 줘서 해결하자"를
시도했다가 **실측으로 기각**했습니다:

- **스킬 시너지에 가중치 부여** → 티어만 깎이고 검증 가능한 시너지는 0건 증가. 기각
- **파트너 조건을 티어보다 우선** → 경고 하나 없애려고 평균 7점 손실. 기각
- **낭비 인원 부분 점수(70%)** → 근거 없는 수치라 채택 안 함

대신 **조건을 밝히고 판단은 사용자에게 넘기는** 방식으로 통일했습니다.
`invest`/`expert`/`partner` 경고가 전부 이 방식입니다(§5-3).

### 2-3. "조용한 누락"이 가장 위험합니다

이 프로젝트에서 실제로 일어난 사고들:

- `name_kr`이 `"{{hover"`(위키 템플릿 잔재)여서 **홍련(PvP SS)이 모든 추천에서 통째로 누락**
- prydwen 조합의 빈 칸 표시를 스크랩 때 버려서 **5인 팀이 2~4인으로 잘려 후보에서 제외**
- 마스트: 로망틱 메이드가 토템 목록에 없어서 **프리카+민트 조합이 한 번도 추천되지 않음**
- 이미지가 `_FB`(전신)여서 네온: 비전 아이만 다른 비율로 표시

전부 **에러 없이 조용히 잘못된 결과**를 냈습니다. 그래서 `scripts/checkData.mjs`가 있습니다.
새 데이터를 넣을 때는 반드시 검사기를 함께 확장하세요.

---

## 3. 데이터 파일

모두 `nikke-site/data/` 아래에 있습니다.

| 파일 | 내용 | 현재 규모 |
|---|---|---|
| `characterDatabase.json` | 캐릭터 상세(스킬 3종, 쿨타임, 버스트, 원소, 클래스, 무기, 제조사, 출시일, 모드별 티어, prydwenTags, **이름 3개 국어 `title`/`name_kr`/`name_ja`**) | 196명 (SSR 168 / SR 19 / R 9) |
| `characters.js` | **화면 캐릭터 선택 그리드가 읽는 UI 목록. SSR 전용** | 168 항목 |
| `synergyNotes.json` | prydwen 아키타입, 시너지 페어, 카운터 | 아키타입 483 / 페어 13 / 카운터 4 |
| `characterInvestmentNotes.json` | 애장품 필요 여부, 투자 우선순위, **토템 역할** | 76건 (토템 18명) |
| `treasureEffects.json` | 애장품 효과 | 17명 (전원) |
| `metaStats.json` | enikk.app 실사용 데이터 | 캠페인 조합 20 / PvP 상위 11 |
| `dataFreshness.json` | 각 파일의 asOf / 만료일 | — |
| `glossary.json` | 커뮤니티 번역용 게임 용어 3개 국어 표기 (§7-3) | 20건 |

### ⚠️ 이미지는 두 파일이 각자 들고 있습니다

`characterDatabase.json`과 `characters.js` **양쪽 모두** `img` 필드가 있습니다.
화면 그리드는 `characters.js` 쪽을 읽습니다. 한쪽만 고치면 화면이 안 바뀝니다(실제로 겪음).
`UI_IMG_MISMATCH` 검사가 이걸 잡아줍니다.

이미지 경로 규칙: **디코딩한 파일명의 MD5 앞 1글자/앞 2글자**.
반드시 `_MI.png`(상반신)를 쓰세요. `_FB.png`는 전신이라 세로형 카드에서 혼자 튑니다.

### 캐릭터 이름 표기 (어기면 데이터가 무시됩니다)

- 항상 `characterDatabase.json`의 **`title` 값을 정확히** 사용. 축약형 금지
- 과거 사고: `Ada`/`Takina`/`Jill`/`Mari`/`Asuka`/`Rita` 축약형 → 데이터 통째로 무시
- 정식 표기: `Ada Wong`, `Takina Inoue`, `Jill Valentine`, `Mari Makinami Illustrious`, `Asuka Shikinami Langley`, `Liter`
- prydwen 표기와 다른 경우: prydwen `Eve` = 우리 `EVE`, prydwen `Asuka Shikinami Langley: Wille` = 우리 `Asuka: WILLE`
- 조건을 이름에 붙이지 마세요. `"Zwei (Treasure)"` 금지 → `requiresTreasure: ["Zwei"]` 필드 사용

---

## 4. 추천 로직 (`lib/synergyEngine.js`)

### 4-1. 선정 경로 — 2026-08-08에 서열을 없앴습니다

```
같은 층에서 점수 비교:
  ① enikk.app 실사용 5인 완전일치   (findRealUsageTeamMatch)
  ② prydwen 아키타입 완전일치        (findExactTeamMatch, 빈 칸 자동 채움 포함)
  → totalScore가 높은 쪽 채택. 동점이면 ① (실제 클리어 기록이 더 강한 근거)
  → 진 쪽은 alternative로 함께 화면에 표시

폴백: 둘 다 없을 때만 recommendTeams (보유 로스터 전체 탐색)
```

**왜 서열을 없앴나:** 유저가 정의를 명확히 함 — "검증된 조합"이란 사람들이 두루두루 쓰는
조합 **또는** prydwen 등록 조합이며 둘은 대등하다. 그런데 enikk을 무조건 위에 두는 바람에,
유저 로스터 70명 기준으로 35점짜리 enikk 조합이 42점짜리 prydwen 조합을 밀어냈습니다.
게다가 그 enikk 조합은 캠페인 실사용 20건 중 **평균 전투력이 가장 낮은(42만, 전체 평균
89.7만) 저투자 계정용** 구성이었습니다. "많이 쓰인다"가 "이 유저에게 좋다"는 뜻이 아닙니다.

### 4-2. 점수 = tierTotal

**`totalScore`는 오직 5명의 모드별 티어 점수 합**입니다.
`TIER_SCORE = { SSS:9, SS:8, S:7, A:6, B:5, C:4, D:3, E:2, F:1 }`

모드→티어 키: `campaign`/`story`/`tribe_tower` → `story`, `bossing`/`raid` → `bossing`, `pvp` → `pvp`

`scoreTeam().totalScore`(가중치 합산)는 계산되지만 **버려집니다.** 아키타입 중복 합산 버그로
700점대까지 부풀 수 있어서, 이해하기 쉽고 안정적인 tierTotal로 대체했습니다.

### 4-3. 낭비(waste) 판정

같은 버스트 단계에 인원이 남으면 뒤 순번은 **0점** 처리됩니다.
`FAST_BURST_CD = 20`(1명이면 커버) / `ALTERNATE_BURST_CD = 45`(2명이면 커버)

⚠️ **이 판정은 0점 아니면 전부라 거칠습니다.** "버스트 못 쓰는 SSS"와 "버스트 못 쓰는 D"를
똑같이 0으로 봅니다. 이게 여러 문제의 근원이었고, `totemRole` 예외로 완화하고 있습니다.
부분 점수(70% 등)를 도입하는 안이 검토됐으나 근거 없는 수치라 보류 중입니다.

### 4-4. 자유 슬롯 (flexSlots) — 채우는 규칙의 변천

prydwen 조합에는 "이 3명 + B3 아무나"처럼 비워둔 자리가 있습니다.
슬롯 종류: `B1` `B2` `B3` `B1-CDR` `flex`

**규칙이 세 번 바뀌었습니다. 이 역사를 모르면 되돌리기 쉽습니다:**

1. **~2026-08-07**: 개인 티어 최고 → God Comp #2에서 레드후드(SSS)를 골라 35점(모더니아면 41점)
2. **2026-08-07**: 기여도(낭비 반영 tierTotal 증가분) → 팀 점수는 좋아졌으나, 낭비 판정이
   0/전부라 저티어가 자주 뽑힘(무작위 133조합 중 260슬롯)
3. **2026-08-08 (현재)**: **칸의 용도로 거르고 그 안에서 최고 티어**
   - 유저 지시: "B3면 3버스트 중 최고, FLEX면 버프류 중 최고"
   - 근거: 티어가 높다 = 범용성이 높다. 조합을 찾아보는 사람은 니케 풀이 좁아 **나중까지 쓸
     캐릭터**가 중요하다
   - **`flex`에만 조건이 없어서 사실상 '아무나'였던 게 핵심 문제였음** → 전 아군 버퍼로 좁힘
     (196명 중 74명). 로스터에 버퍼가 없으면 조건을 풀어 아무나 채움
   - 검증: 무작위 133건 전수탐색 대조, 슬롯 조건 내 티어 합 최대 **133/133 (100%)**

티어를 우선한 대가(버스트를 못 쓰는 자리)는 `[토템 활용]` 또는 낭비 경고로 **근거에 명시**됩니다.

**2026-08-09: 빈칸 차감(`FLEX_SLOT_PENALTY = 1`) 추가 — 비교용에만.**

빈칸은 로스터에서 조건에 맞는 **최고 티어**로 채워집니다. 즉 빈칸 조합은 고정 5인 조합과 달리
**로스터 전체를 뒤져 최댓값을 고르는** 셈이라 티어 합 비교에서 구조적으로 유리합니다
(max-of-N 이득). 유저 지적으로 드러났습니다 — "베스티: 택티컬 업(story SSS) 하나만 고정이고
빈칸이 4개인 조합이 있는데, 이 니케를 보유하면 무조건 그 조합이 선정될 가능성이 크다."

빈칸 있는 아키타입은 360개 중 **15개뿐**인데(4칸 3 / 3칸 5 / 2칸 4 / 1칸 3),
캠페인 완전일치 1위의 **85%** 를 이 15개가 가져가고 있었습니다. 빈칸 조합 15개 중 10개가
campaign 모드라 **보스전·PvP는 거의 영향이 없습니다.**

**실측 (베스티 보유 로스터 49건, campaign):**

| 로스터 | 원맨아미 1위 | 다른 완전일치가 있었나 | 그때 점수차 |
|---|---|---|---|
| 30명 | 100% | **0건** | — |
| 50명 | 92% | **0건** | — |
| 70명 | 67% | 8건 중 1건 | 11점 |
| 100명 | 도입 전 63% → **17%** | 5건 중 4건 | **평균 2.8점 (1~6)** |
| 168명 | 도입 전 100% → **0%** | 1건 중 1건 | 1점 |

**두 문제가 섞여 있었습니다.** 좁은 로스터(30~50명)에서 원맨아미가 이기는 건 **다른 완전일치가
아예 0건**이라 경쟁자가 없어서입니다 — 차감으로는 절대 안 바뀝니다(바꿀 대상이 없음).
넓은 로스터에서만 대안이 생기고, 그 구간의 점수차가 평균 2.8점이라 **빈칸 4칸 × 1점 = 4점**이
정확히 맞는 크기입니다. 전체 결과는 76% → 67%이지만, 100명 이상 구간에서는 크게 뒤집힙니다.

⚠️ **1점은 잠정값입니다.** 유저가 "몇 점이 맞는지는 아직 정하지 못했다"고 했습니다.
바꿀 때는 `FLEX_SLOT_PENALTY` 상수만 고치고 실측을 다시 돌리세요. **0으로 두면 도입 전 동작과
완전히 같습니다.**

**차감은 후보 비교(`cmpScore`)에만 쓰고 화면 표시 점수(`totalScore`)에는 반영하지 않습니다.**
표시 점수가 "이 5명의 실제 티어 합"이라는 의미를 잃으면 사용자가 해석할 수 없기 때문입니다.

**조사 중 발견 — 원맨아미 3개는 사실상 중복 등록입니다.**
고정 1명 + 빈칸 4칸짜리는 `Vesti: Tactical Upgrade (aka Vesgod)` / `Other One Nikke Army Teams`
(라피: 레드 후드) / `Solo Anis: Star` 3개인데, 전부 prydwen이 **"얘는 혼자 캐리한다"고 써둔
서술**을 조합처럼 등록한 것입니다. 그런데 같은 캐릭터의 확정 조합이 이미 많습니다 —
베스티 26건(그중 `One-Nikke-Army-Vesgod`라는 확정판까지 존재), 라피: 레드 후드 98건,
아니스: 스타 50건. `DUP_ARCHETYPE` 검사는 키가 (모드+멤버+flexSlots)라 이걸 못 잡습니다.
차감으로 부족하다고 판단되면 **이 3개를 후순위로 미루거나 후보에서 빼는 안**이 다음 카드입니다.

### 4-5. 토템 (totemRole) — 24명

'토템'은 **버스트를 쓰지 않는 자리에 놓는 니케**입니다(인벤 2025-07-24 정의).
`totemRole`이 등록된 캐릭터만 낭비 판정에서 면제됩니다.

```
Helm, Nayuta, Mast: Romantic Maid, Prika, Privaty, Ada Wong,
Anis: Sparkling Summer, Ludmilla: Winter Owner, Elegg: Boom and Shock,
Rouge, Soline: Frost Ticket, Ade: Agent Bunny, Naga, Modernia,
D: Killer Wife, Tia, Noir, Rei Ayanami (Tentative Name),
Miranda, Crust, Zwei, Drake, Maiden: Ice Rose, Rei Ayanami   ← 2026-08-09 추가
```

**2026-08-09 추가한 6명** — `findTotems.mjs` 2군을 스킬 원문으로 전수 검토한 결과입니다.
판정 기준은 **아군 버프의 발동 조건이 "자기 버스트 사용"이 아닐 것**(기존 18명이 통과한 기준).

| 캐릭터 | 발동 조건 | 대상 |
|---|---|---|
| 미란다 | 풀버스트 시작 / 평타 30회 | 전 아군 (크리티컬 데미지 ▲19.49%) |
| 크러스트 | 평타 3회 / 풀차지 3회 | 전 아군 (공격력·방어력 + 디버프 해제) |
| 츠바이 | 풀버스트 진입 | 전 아군 (크리 확률 ▲11.01%, 관통) |
| 드레이크 | 풀버스트 시작 | 전 아군 (명중률·공격력 ▲7%) |
| 메이든 : 아이스 로즈 | MP 충전(버스트 단계 진입) | **전기 아군만** |
| 아야나미 레이 | 버스트3 단계 진입 | **작열 아군만** |

**아르카나는 검토 후 제외했습니다.** 버스트 없이 나오는 건 공격력 ▲2.95% / 공격 데미지
▲4.43%뿐이고, 값어치인 ▲106.36% 버프들은 전부 *"자신이 Wheel of Fortune 상태일 때"*
조건인데 그 상태를 **자기 버스트 스킬**이 겁니다(아르카나는 전기라 자기 버스트가 자신에게도
걸림). 7명 중 버스트가 가장 필요한 캐릭터라 토템으로 등록하면 안 됩니다.

**`totemCondition`(속성 한정) 4명**: 아니스: 스파클링 서머(전기), 일레그: 붐 앤 쇼크(수냉),
메이든: 아이스 로즈(전기), 아야나미 레이(작열). 버프가 해당 속성 아군에게만 들어가므로
팀에 그 속성 아군이 없으면 면제받지 못합니다.

양방향 실측(2026-08-09):
- 전기 아군 있음 → 메이든이 버스트3에서 밀려도 `[토템 활용]`로 면제 (100점)
- 전기 아군 없음 → 같은 구조인데 `⚠️ 사실상 낭비`로 판정 (92점)

**아야나미 레이 두 명은 서로 다른 캐릭터입니다.** 가칭은 풍압/AR(2025-02-27), 이쪽은
작열/MG(2024-08-29)이고 스킬이 전혀 다릅니다. 가칭의 버프는 *"전 아군"* 대상이라 조건이
없고, 이쪽은 *"작열 코드 아군"* 이라 조건이 붙습니다. 한쪽만 등록돼 있던 건 누락이 아니라
**스킬 차이에 따른 정상 상태**였습니다.

**마스트: 로망틱 메이드는 엄밀히 토템이 아닙니다.** 버스트를 안 쓰는 게 아니라 **취기 스택 3을
모은 뒤에 씁니다.** 크라운(쿨20초)이 1·2사이클을 돌리고 3사이클째에 마스트가 터뜨리는
**크크마(크라운-크라운-마스트)** 로테이션이 정석입니다. 점수 처리는 totemRole이 맞지만
설명이 틀려서 정정했습니다. 유저의 게임 지식으로 발견된 케이스입니다.

**⚠️ totemRole 추가는 항상 사람이 판단합니다.** 추천 점수를 직접 바꾸는 값이고, 과거
레드 후드를 "버스트 단계가 all"이라는 **허위 근거**로 등록한 사고가 있었습니다.
`totemNote`에 반드시 출처를 남기세요(`[스킬 원문 근거]` 표기도 허용).

**⚠️ totemRole은 "토템으로**도** 쓸 수 있다"는 뜻입니다 (2026-08-09, 유저 지적).**

토템으로만 써야 한다는 뜻이 아닙니다. 드레이크는 샷건덱에서 딜러로 활약합니다.
그래서 토템 설명은 **그 캐릭터가 실제로 버스트 순번에서 밀렸을 때만** 나와야 합니다.

실제로 이걸 어기고 있었습니다. `scoreTeam` 안에 토템 설명 경로가 둘이었는데, 옛 경로가
`같은 버스트 단계에 쿨 20초짜리 동료가 있는가`만 보고 **이 캐릭터가 밀렸는지를 확인하지
않았습니다.** 결과가 이렇게 나왔습니다:

```
⚠️ Diesel는(은) ... 버스트를 발동할 기회가 거의 없고 ... 사실상 낭비
[토템 활용] Nayuta는(은) ... 매 사이클 버스트를 쓰지는 못하지만 ...
```

나유타(B2 쿨20)와 디젤(B2 쿨20)이 같이 있으면 티어가 높은 **나유타가 남고 디젤이 밀리는데**,
정작 버스트를 돌리는 나유타한테 "버스트를 못 쓴다"고 설명한 것입니다. 한 결과 안에서
서로 모순되는 두 문장이 나왔습니다.

같은 문제가 생길 수 있던 토템 5명(전부 쿨 20초): 루주 / 나가 / D: 킬러 와이프 / 나유타 /
에이드: 에이전트 바니.

옛 경로를 지웠습니다. `totemExempted`(실제로 밀린 멤버) 경로가 이미 제대로 설명하므로
기능 손실은 없고, **점수는 바뀌지 않습니다**(설명 문구만 잘못 나오던 것). 실측으로
확인했습니다 — 나유타 조합 78.5점 유지, 실제로 밀린 토템(티아·나가)의 설명은 그대로 출력.

### 4-6. `scripts/findTotems.mjs` — 누락된 토템 자동 검출

실사용 조합을 엔진에 돌려 **"버스트 순번에서 밀리는데도 반복 기용되는 캐릭터"**를 찾습니다.
논리: 유저 수만 명이 굴리는 조합에서 우리 엔진이 "자리를 낭비한다"고 하면, 우리가 틀렸을
가능성이 높습니다.

- **1군**(PvE 실사용 + 스킬 근거) = 등록 유력. 현재 0명
- **2군**(공략 권장 조합만) = 근거 약함. 현재 **1명**(아르카나 — 검토 후 등록 안 함, §4-5 참고)
- **PvP 전용은 근거로 쓰지 않습니다** — 선공 한 방 승부라 버스트 순환 전제가 다릅니다

이 스크립트가 마스트: 로망틱 메이드를 PvE 가중치 40.90(2위 헬름 4.18)으로 잡아냈습니다.

---

## 5. prydwen 특수 표시 (prydwenTags) — 79명

prydwen 티어리스트가 캐릭터 아이콘에 붙여둔 라벨을 그대로 옮긴 값입니다.

| 태그 | 원문 | 뜻 | 인원 |
|---|---|---|---|
| `limited` | is a limited character that isn't available in the general pool | 한정 | 50 |
| `partner` | can only shine if a specific unit is in the team | 특정 동료 필요 | 22 |
| `invest` | heavy investment is required | 고투자 전제 | 16 |
| `expert` | requires high manual skill | 수동 조작 숙련 | 7 |

수집: https://www.prydwen.gg/nikke/tier-list 의 `div.avatar-card` → `.emp-name` + `.new-tag`(클래스명이 곧 태그)
`treasure` 태그는 `characters.js`의 `hasTreasure`로 따로 다루므로 넣지 않습니다.
`X (Treasure)` 항목에만 붙은 태그는 `prydwenTagsTreasure`로 분리(Zwei, Tove).

### 5-1. `partner`의 짝은 데이터에서 계산합니다

태그는 "조건부"라는 사실만 알려주고 **누가 파트너인지는 안 알려줍니다.**
그래서 조건부 확률로 뽑습니다 — "이 캐릭터가 나온 팀 중 몇 %에 저 캐릭터가 같이 있었나".
단순 동시 등장 횟수는 크라운·아니스 스타 같은 범용 픽이 항상 1등이라 무의미합니다.

최소 5팀 등장 + 70% 이상만 인정 → 6명:
`Prika→Mint 100%`, `Tia→Naga 100%`, `Arcana↔Isabel 100%`,
`Emma:TU→Eunhwa:TU/Vesti:TU 100%`, `Velvet→Little Mermaid 73%`

### 5-2. 사용 방식 — 점수를 깎지 않습니다

`invest`/`expert`/`partner` 모두 **근거 문장에 `[조건 확인]`으로 표시만** 합니다.
유저 투자 수준을 모르는 상태에서 감점하면 이미 키운 사람에게 틀린 추천을 하게 됩니다.
AI 프롬프트에도 `[조건 확인]` 항목은 반드시 포함하되 겁주지 말라고 명시했습니다.

---

## 6. AI 설명 (`app/api/ai-recommend/route.js`)

### 비용 구조 (2026-08-08 측정)

| | 1건 | 일 1,000건 월 |
|---|---|---|
| 변경 전 (Sonnet 5 + thinking, 2,573토큰) | 19.2원 | 57.6만원 |
| **현재 (Haiku 4.5, 1,995토큰, thinking 제거)** | **4.9원** | **14.7만원** |

**75% 절감.** 여기에 캐시 적중분은 추가로 제외됩니다.

- 모델은 `AI_EXPLAIN_MODEL` 환경변수로 교체 가능 (`claude-sonnet-5` 넣으면 원복)
- `thinking`/`output_config` 제거 — thinking 토큰은 출력 요금으로 청구되어 비용의 절반가량 차지
- 프롬프트로 보내는 근거 문장은 250자, 아키타입 노트는 400자로 캡 (화면 원본은 그대로)
- 아키타입 노트가 근거 문장과 noteBlock에 **두 번** 실려 나가던 것 제거

### 캐싱 (`ai_explain_cache` 테이블)

엔진이 결정적이라 같은 입력 → 같은 출력입니다. **캐시 키는 로스터가 아니라 "AI에게 실제로
보낸 입력"의 SHA-256**(모드/언어/멤버/근거/노트). 그래서:
- 서로 다른 로스터라도 결론이 같으면 캐시 공유
- 엔진·데이터가 바뀌어 근거가 달라지면 키가 자동으로 바뀜 → **수동 무효화 불필요**
- 프롬프트를 크게 바꿀 때만 `buildCacheKey`의 `v` 값을 올림 (현재 v2)

실측: 미스 8.5초 → **히트 1.9초**, 비용 0.

주의 두 가지:
- **캐시 적중은 하루 사용 횟수를 깎지 않습니다** (비용이 0이므로)
- **AI 호출 실패 시 폴백 문장은 캐시에 저장하지 않습니다** (저장하면 그 조합에 열화된 설명이 영구히 박힘)

### 레이트리밋

`DAILY_LIMIT = 30` (IP 해시 기준, `ai_explain_usage` 테이블)
검사와 증가가 분리되어 있습니다 — 진입 시 한도만 읽고, 실제 API 호출 뒤에만 증가.

### 일일 총 호출 상한 (서킷 브레이커) — 2026-08-09 도입 완료

IP당 30회 제한만으로는 IP가 많아지면 총액을 못 막습니다. 전역 상한을 넣었습니다.

- `DAILY_GLOBAL_LIMIT` = `AI_DAILY_GLOBAL_LIMIT` 환경변수, **기본 1,000회/일**
  (1건 약 4.9원 → 하루 약 4,900원 / 월 약 14.7만원. "감수할 금액"이 아니라 **폭주를 끊는 천장**)
- **캐시 적중은 세지 않고 막지도 않습니다.** 상한 확인은 캐시 조회 뒤, AI 호출 직전에만 합니다
- **상한에 걸려도 에러를 내지 않습니다.** `fallbackReasoning`(근거 문장)으로 대체하고
  응답에 `budgetExhausted: true`를 실어 화면에 안내를 띄웁니다. 조합 구성·점수·근거는 전부
  엔진이 정하므로(§2-1) AI 문장이 빠져도 결과물의 핵심은 그대로입니다
- 이 폴백 문장은 **캐시에 저장하지 않습니다**(기존 규칙과 동일)

**테이블: `ai_daily_budget`** (`supabase/ai_daily_budget_migration.sql`, **2026-08-09 실행 완료**)

```sql
ai_daily_budget(usage_date date primary key, count integer)
increment_ai_daily_budget()  -- security definer, 원자적 upsert 증가
```

**왜 RPC로 증가시키나:** select 후 update 방식은 동시 요청에서 갱신이 유실돼 카운터가 실제보다
적게 남습니다. 서킷 브레이커에서 그건 "보호가 조용히 약해지는" 방향이라 특히 나쁩니다.

**왜 select 정책만 열었나:** anon 키는 프론트 번들에 그대로 들어갑니다. insert/update를 열면
누구나 카운터를 0으로 만들어 상한을 우회하거나, 반대로 큰 값을 넣어 서비스를 막을 수 있습니다.
증가는 `security definer` 함수로만 가능합니다.
**실제로 anon 역할로 확인했습니다** — RPC 호출은 되고, 직접 update/insert는 RLS가 막습니다.

⚠️ **조회 실패 시에는 열어둡니다**(Supabase가 흔들렸다고 사이트가 멈추는 건 과함).
대신 `[AI_BUDGET]` 태그로 로그를 남깁니다. **배포 후 이 태그가 반복해서 찍히면 보호가 안 걸리고
있다는 뜻이니 즉시 확인하세요.**

---

## 7. 운영 환경

### Supabase (프로젝트 `yttfwroeyplwrchyitud`)

마이그레이션은 `nikke-site/supabase/` 아래. **전부 실행 완료 상태**입니다.

| 파일 | 용도 |
|---|---|
| `schema.sql` | 기본 테이블 + RLS |
| `treasure_migration.sql` | `owned_nikke.has_treasure` 컬럼 |
| `ai_rate_limit_migration.sql` | `ai_explain_usage` (이름이 옛 기능을 가리키지만 용도는 레이트리밋) |
| `ai_explain_cache_migration.sql` | `ai_explain_cache` (2026-08-08 실행) |

### Vercel 환경변수

```
NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_ANON_KEY
ANTHROPIC_API_KEY
NEXT_PUBLIC_ADSENSE_CLIENT_ID        = ca-pub-1541956672617594
NEXT_PUBLIC_ADSENSE_SLOT_BANNER      = 7234519961
NEXT_PUBLIC_ADSENSE_SLOT_RECTANGLE   = 5842768434
(AI_EXPLAIN_MODEL — 선택. 넣으면 모델 교체)
```

### ✅ 구글 로그인 — 완료 (2026-08-09)

**로그인이 구글 OAuth로 전환됐고 정상 동작 확인했습니다.** 이메일 발송 한도 문제는 이걸로
해소됐습니다(구글 로그인은 메일을 한 통도 쓰지 않습니다).

| 항목 | 값 |
|---|---|
| 구글 클라우드 프로젝트 | `my-project-34158nikke-site` (니케 전용, **BizGlitch와 별개**) |
| 앱 이름(동의 화면 표시) | **니케 조합 추천** |
| 게시 상태 | **프로덕션** — 구글 계정 있는 누구나 로그인 가능 |
| OAuth 클라이언트 | 웹 애플리케이션 1개 |
| 리디렉션 URI | `https://yttfwroeyplwrchyitud.supabase.co/auth/v1/callback` |
| Supabase Provider | Google **Enabled** |

**기존 계정이 그대로 이어졌습니다(실측):**
`jyd7661@gmail.com`의 `auth.identities`에 **`email, google` 두 개가 같은 user_id로** 붙었고,
보유 니케·운영자 권한이 유지됩니다. 별도 계정이 생기지 않았습니다.

**Supabase 구글 설정은 원래 다른 값이 들어 있었습니다.** 이 프로젝트를 배당앱과 공유하던
시절의 잔재로, 쇼츠·배당·블로그 세션 모두 "자기 것 아니다"라고 확인해줘서 덮어썼습니다.
토글이 꺼져 있어 사용 중이 아니었습니다.

**남은 정리**: 매직링크(`sendLink`)가 `components/Header.js`에 "이메일로 로그인"으로 접혀
있습니다. 구글이 검증됐으니 지워도 됩니다. 지우면 `signInWithOtp` 관련 상태(email/sent)도
함께 정리하세요.

⚠️ **구글 클라우드 프로젝트 표시 이름이 `My Project 34158nikke-site`로 잘못 지어졌습니다.**
(생성 시 입력이 기본값 뒤에 붙음) 내부용이라 기능엔 영향 없지만 정리하면 좋습니다.
사용자에게 보이는 이름은 동의 화면의 **니케 조합 추천**이라 문제없습니다.

### (해결됨) 로그인 이메일 시간당 2통 한도 — 구글 로그인 전환으로 우회

**2026-08-09에 실제로 막혔습니다.** 로그아웃/로그인을 몇 번 반복하니 매직링크가 안 오고
Supabase 인증 로그에 `429: email rate limit exceeded` / `over_email_send_rate_limit`이 찍혔습니다.

원인: 로그인이 `signInWithOtp`(이메일 매직링크)인데, 지금 메일을 보내는 주체가
**Supabase 내장 메일 서비스**(`noreply@mail.app.supabase.io`)입니다. 이건 **개발용**이고
공식 문서도 *"availability is on a best-effort basis. For production use, you should consider
configuring a custom SMTP server"* 라고 못박고 있습니다. 실측으로 **시간당 2통**입니다
(10:11:50, 10:13:04 발송 성공 → 그다음부터 전부 429).

**⚠️ 이 한도는 계정별이 아니라 프로젝트 전체 공유입니다.** 즉 **지금 상태로 공개하면
하루 몇 명만 로그인해도 나머지 사용자는 아예 로그인을 못 합니다.** 광고를 붙이기 전에
반드시 해결해야 합니다 — 애드센스 통과보다 이게 먼저입니다.

**해결**: Supabase 대시보드 → Authentication → Emails → SMTP Settings 에 커스텀 SMTP 등록
(SendGrid, AWS SES, Resend 등). 등록하면 기본 한도가 **시간당 30통**이 되고,
Authentication → Rate Limits 에서 더 올릴 수 있습니다.

**개발 중 우회**: 비로그인 화면을 확인할 때 **로그아웃하지 말고 시크릿 창을 쓰세요.**
로그아웃하면 다시 들어올 때 메일을 한 통 더 쓰게 되고, 두 번이면 한 시간 묶입니다.

### 애드센스 — 사이트 검토 대기 중

계정 승인은 났지만 `nikke-site.vercel.app`은 **`준비 중`(사이트의 광고 게재 가능 여부 검토 중)**
상태입니다. 그래서 `<ins>`는 정상 렌더되고 `status: done`인데 광고가 비어 있습니다.
**우리 쪽 연동 문제가 아닙니다.** 통과되면 `준비됨`으로 바뀝니다.

남은 항목: **동의 메시지(CMP)** — EEA·영국·스위스 방문자용 쿠키 동의 배너. 미설정 시 그 지역
광고 수익이 빠집니다. 3가지 선택형(동의/동의하지 않음/옵션 관리)이 GDPR 요건에 가장 안전.
**법적 성격이 있어 사용자 결정이 필요합니다.**

### 예약 작업 `nikke-site-data-research`

매주 월요일 10:04 실행. 프롬프트 위치: `C:\Users\정연도\Claude\Scheduled\nikke-site-data-research\SKILL.md`
(읽기 전용. 수정은 `mcp__scheduled-tasks__update_scheduled_task`로)

핵심 규칙:
- **A(그대로 베낀 값)만 자동 반영, B(추론·해석)는 제안서에만**
- `totemRole` 신규 등록은 항상 B
- **예약 작업은 git 커밋·푸시를 하지 않습니다.** 파일만 고치고 커밋은 사람이 diff를 보고 직접.
  (사람 없이 백그라운드로 돌기 때문이며, 샌드박스엔 자격증명도 없습니다)
  ※ 이건 **예약 작업에만** 해당합니다. 일반 세션은 GitHub Desktop으로 정상 커밋·푸시합니다 — §8 참고
- 매주 `findTotems.mjs` + `checkData.mjs` 실행
- 보고서는 `<클론>/reports/YYYY-MM-DD.md`

**⚠️ 프롬프트 파일에 접근이 안 됩니다.** `C:\Users\정연도\Claude`는 Claude 앱 데이터 폴더라
Cowork 폴더 공유가 거부됩니다(하위 폴더도 마찬가지). 읽으려면 사용자가 파일을 직접 첨부해야 합니다.
2026-08-08에 그렇게 받아 확인했고, 아래 두 가지를 고쳐 돌려드렸습니다:

- **경로 오류**: `/sessions/<세션>/mnt/Desktop/nikke-site-git/`로 적혀 있었으나 실제로는
  `/sessions/<세션>/mnt/nikke-site-git/`. 마운트는 연결된 폴더 이름 기준이라 `Desktop/`이 안 들어갑니다
- **오버스펙 점검 절차 추가**: 신규 SSR 출시 시 오버스펙 여부를 확인해 제안(B)으로 올리는 항목.
  `TOWER_NON_OVERSPEC` 검사는 prydwen이 그 캐릭터를 필그림 타워 조합에 넣어준 뒤에야 발화하므로
  출시 직후 몇 주의 공백을 사람이 메워야 합니다

---

## 7-1. ⚠️ 컴퓨터 제어 전 순번 잠금 (v2.1, 2026-08-09 확정)

**사용자가 코워크 세션을 4개 운영하며 전부 같은 컴퓨터를 제어합니다.** 실측 확인: 모든 세션이
같은 리눅스 VM을 쓰고(`ls /sessions/`로 서로 보임), `ListAgents`로는 서로 도달할 수 없습니다.
그래서 **공유 파일로 순번을 잡습니다.**

**이 세션의 이름은 `니케`입니다.** (다른 세션: `배당앱` / `블로그` / `쇼츠`)

전체 규칙 원본: `C:\Users\정연도\Downloads\코워크-컴퓨터제어-조율규칙.md`
스크립트: 같은 폴더의 `cowork-lock.sh` — **수정 금지.** 4개 세션이 공유하므로 고칠 것이
있으면 사용자를 통해 작성자 세션에 전달합니다.

```bash
DL=$(ls -d /sessions/*/mnt/Downloads | head -1); cd "$DL"
bash cowork-lock.sh acquire "니케" "<하려는 작업>"   # ACQUIRED 면 진행, WAIT 면 대기
bash cowork-lock.sh renew   "니케" "<현재 단계>"     # 15분 넘는 작업이면 10분마다 (필수)
bash cowork-lock.sh release "니케"                   # 끝나면 즉시
bash cowork-lock.sh status                           # 점유자 + 대기열 (잠금 없이 호출 가능)
```

**화면 조작(`computer_*`)을 했다면 `release` 와 함께 `computer_release_lock` 도구도 호출하세요.**
파일 잠금은 협조적 락이라 강제력이 없고, 실제로 다른 세션의 조작을 막는 건 기기 내장 락입니다.
이걸 빼먹으면 다른 세션은 파일상 FREE인데 조작이 안 되는 상태가 됩니다.

### 이 프로젝트에서 실제로 잠금이 필요한 순간

- **필요**: `Downloads` 폴더 작업(조율 파일 자체), `computer_*` 화면 조작
- **대체로 불필요**: `nikke-site-git` 작업. 이 저장소는 이 세션 전용이라 다른 세션과
  같은 파일을 건드리지 않습니다. 다만 다른 세션이 이 폴더를 만질 여지가 생기면 잡으세요
- **불필요**: 클라우드 컨테이너의 `Bash`/`Read`/`Write`, 웹 검색, MCP 커넥터

### v2.1 스크립트로 실측한 함정 (규칙 문서에 없는 내용)

1. **잠금을 이미 쥔 상태에서 `acquire`를 다시 부르지 마세요.** 자기 대기표가 맨 뒤로
   재등록돼 `WAIT | 앞 순번 …`이 돌아옵니다. **실제 소유자는 여전히 나**인데 "못 잡았다"고
   오판하면 release를 안 하게 되고 15분 TTL까지 아무도 못 씁니다.
   한 턴에 한 번만 잡고, 이어서 할 일이 있으면 `renew`를 쓰세요.
   (규칙 문서의 검증 11개 항목에 이 시나리오는 없습니다)
2. **작업 설명에 큰따옴표(`"`)를 넣지 마세요.** JSON이 깨져 `status`가 작업명을 앞부분만
   읽습니다. 실측: `엔진 "빈칸" 검증` → `엔진 `로 잘림
3. **스크립트 시험은 반드시 `/tmp` 사본에서.** 실제 공유 파일에서 테스트하면 남의 잠금이
   지워집니다. 2026-08-09 00:46에 이 세션이 보유하던 잠금이 실제로 그렇게 해제됐습니다

## 8. 개발 환경의 함정 (실제로 겪은 것들)

### ✅ 커밋은 금지가 아닙니다 — 방법이 정해져 있을 뿐입니다

혼동이 실제로 있었어서 먼저 적습니다. **일반 세션은 커밋·푸시를 정상적으로 합니다.**
금지된 것은 **샌드박스(bash)에서 `git` 명령을 직접 실행하는 것**뿐입니다.

```
파일 수정   →  Read/Write/Edit 또는 샌드박스 bash
커밋·푸시   →  GitHub Desktop을 computer-use로 조작 (Summary/Description 입력 후 Commit → Push origin)
```

이 방식으로 지금까지 20회 이상 배포했습니다. §7의 "예약 작업은 커밋하지 않는다"는
**백그라운드로 도는 예약 작업에만** 해당하는 규칙입니다.

| 함정 | 대응 |
|---|---|
| **npm registry 403 차단(기기 쪽)** | 사용자 컴퓨터 VM에서는 `npm install` 불가. **단 클라우드 컨테이너에는 네트워크가 있어 `npm i esbuild` 로 JSX 문법 검증이 됩니다**(2026-08-09 확인). `device_stage_files`로 파일을 올린 뒤 `esbuild <파일> --loader:.js=jsx --outfile=/dev/null`. 문법만 보는 것이라 타입·런타임 오류는 여전히 Vercel 빌드에서만 잡힙니다 |
| **샌드박스에서 파일 삭제 불가** | `mcp__cowork__allow_cowork_file_delete` 호출로 권한 요청 |
| **샌드박스 git 사용 금지 — 조회 명령도 포함** | `.git/index.lock`이 남아 GitHub Desktop 커밋이 `Commit failed - A lock file already exists`로 막힙니다. **2026-08-09에 실제로 당했습니다** — `git log`/`git config`만 볼 생각이었는데 `git status`·`git diff`가 섞였고, 그게 도중에 중단되면서 락이 남았습니다. **`git` 은 조회용으로도 치지 마세요.** 상태 확인이 필요하면 GitHub Desktop 화면을 보거나, `cat .git/refs/heads/main` 과 `cat .git/refs/remotes/origin/main` 을 비교하세요(같으면 푸시 완료). **복구법**: 샌드박스는 파일 삭제가 안 되므로 `mv .git/index.lock .git/index.lock.stale` 로 치우면 됩니다 |
| **CRLF 문제** | 리눅스 쪽 `git status`가 전 파일을 수정됨으로 표시. GitHub Desktop은 정상 |
| **엔진을 node로 직접 못 부름** | `synergyEngine.js`의 JSON import에 `with { type: 'json' }`가 없음. 테스트할 땐 sed로 임시 복사본을 만들고 **끝나면 반드시 삭제** (한 번 커밋에 섞일 뻔함) |
| **`javascript_tool` 출력 차단** | `[BLOCKED: Cookie/query string data]`. 작은 파생 요약만 반환하세요 |
| **Chrome은 computer-use 불가** | tier "read". 브라우저 조작은 `mcp__claude-in-chrome__*` 사용 |

---

## 9. 지금 열려 있는 작업

### 진행 예정: 기업 타워 모드 추가

`tribe_tower` 모드와 아키타입 30개가 **이미 있습니다.** 화면에 탭만 없는 상태.

타워 규칙 (prydwen 가이드 확인 완료):
```
트라이브 타워(일반)   제조사 제한 없음, 상시
엘리시온/미실리스/테트라/필그림   해당 제조사만, 요일제
  월 전체 / 화·금 엘리시온 / 수·토 미실리스 / 목·일 테트라 / 수 필그림
```

**오버스펙 플래그 — 2026-08-08 추가 완료.**

오버스펙은 **3대 기업 캐릭터의 파워업 버전**으로, **소속 기업을 그대로 유지**하면서
**필그림/오버스펙 타워에도** 들어갑니다. 즉 원 소속 기업 타워 + 필그림 타워 **양쪽 사용 가능**
(gamechosun 2025-01, fandom Tribe Tower). 라피: 레드 후드 출시와 함께 필그림 타워가
'필그림/오버스펙 타워'로 개편된 것이 근거입니다.

`characterDatabase.json`에 `overspec: true` + `overspecNote`(출처) 추가 — **4명**:

| 캐릭터 | 소속 | 버스트 | 출시 | 근거 |
|---|---|---|---|---|
| Rapi: Red Hood | elysion | B3 | 2025-01-01 | 최초의 오버스펙 (gamechosun) |
| Mihara: Bonding Chain | missilis | B3 | 2025-05-01 | 2번째 (inven) + prydwen 필그림 조합 편성 |
| Anis: Star | tetra | B1 | 2026-04-23 | inven 3.5주년 "테트라라인 소속 오버스펙 니케" |
| Neon: Vision Eye | missilis | B3 | 2026-04-30 | inven 3.5주년 "네온의 오버스펙 버전" |

**아닌 것으로 확인된 것들 (같은 실수 반복 금지):**
- **아비스타** — 3.5주년 동시 출시라 헷갈리지만 풀돌 배포 일반 SSR. squad도 Overseer
- **앵커: 이노센트 메이드 / 델타: 닌자 시프** — SR의 SSR 버전이지만 **코스튬 알트**. 오버스펙 아님
  ("SR이 SSR 버전을 받으면 오버스펙"이 아닙니다. 미하라가 **2번째** 오버스펙인데
   앵커: 이노센트 메이드가 더 먼저 나왔다는 사실이 이걸 증명합니다)
- **아니스: 스파클링 서머 / 네온: 블루 오션** — 2023년 수영복 알트. 오버스펙 아님

**네온: 비전 아이는 소속이 바뀌었습니다.** 원본 네온은 엘리시온인데 비전 아이는 **미실리스**입니다
(nikke.gg "Missilis Overspec", 엘리시온 타워 사용 불가). DB 값이 맞으니 고치지 마세요.

필그림 타워 가용 인원 27명 → SSR 기준 B1 5 / B2 6 / B3 16.
오버스펙 없이는 B1이 4명뿐이었습니다.

작업 순서:
1. ~~오버스펙 니케 목록 수집 → DB 플래그 추가~~ **완료 (2026-08-08)**
2. ~~엔진에 제조사 필터~~ **완료 (2026-08-08)**
3. ~~필그림 타워 예외 (제조사 OR 오버스펙)~~ **완료 (2026-08-08)**
4. ~~화면에 타워 탭 + 제조사 선택~~ **완료 (2026-08-09)**
5. ~~checkData 검증 추가~~ **완료** — 아래 참고

### 4단계 구현 (2026-08-09) — 기업 타워 모드 추가 완료

- `components/ResultPanel.js` — `AI_MODES`에 `tribe_tower`('타워') 추가.
  `TOWER_OPTIONS`(일반/엘리시온/미실리스/테트라/필그림)를 보스 약점 속성과 같은 방식으로 렌더.
  버튼에 요일을 함께 표기(월 전체 / 화·금 엘리시온 / 수·토 미실리스 / 목·일 테트라 / 수 필그림
  — prydwen 가이드 2026-08-09 재확인)
- `app/page.js` — `tower` 상태 추가, ResultPanel에 전달
- `app/api/ai-recommend/route.js` — body에서 `tower` 수신 →
  **`TOWER_CORP_SET`으로 검증**(엉뚱한 값이 오면 로스터가 통째로 걸러져 원인 파악이 어려움) →
  `matchOpts.tower` / `recommendTeams`에 전달. `buildCacheKey`에 `tower` 포함.
  `modeLabel`은 타워 선택 시 `"엘리시온 타워"`처럼 바뀜

**요일 강조 표시는 일부러 넣지 않았습니다.** SSR과 클라이언트의 날짜가 어긋나면 하이드레이션
불일치가 나는데 로컬 빌드 검증이 제한적이라, 요일은 고정 문구로만 알립니다.

**실측 (전 SSR 168명, `FLEX_SLOT_PENALTY=1` 적용 상태):**

| 타워 | 출전 | 완전일치 |
|---|---|---|
| 일반 | 168명 | Pure Evil Team #1 (44점) |
| 엘리시온 | 43명 | El-Macho (39점) |
| 미실리스 | 34명 | Neon Explodes Mobs in Missilis Tower (32점) |
| 테트라 | 51명 | Tetra Best Tribe Tower Team (37점) |
| 필그림 | 27명 | Pilgrims Best Tribe Tower Team (35점) |

로스터가 부족할 때 문구도 확인했습니다 — "필그림/오버스펙 타워에 출전할 수 있는 버스트
1, 2, 3 캐릭터가 없어…(오버스펙 니케 포함)".

**checkData `TOWER_LIST_DRIFT` 추가.** 타워 목록이 엔진·API·화면 세 곳에 각각 적혀 있어
한 곳만 고치면 조용히 어긋납니다(화면에만 추가 → API가 걸러 필터가 안 걸림 / 엔진에만 추가 →
사용자가 고를 수 없음). 셋을 대조해 다르면 ERROR. **역테스트로 발화 확인함.**

**남은 것**: `app/api/ai-recommend/feedback/route.js`에는 `tower`를 안 넘깁니다.
피드백 테이블 스키마를 바꿔야 해서 보류했습니다 — 타워별 만족도를 보려면 마이그레이션이 필요합니다.

### 2·3단계 구현 (2026-08-08)

`lib/synergyEngine.js`에 `TOWER_CORPS` / `TOWER_LABEL` / `isTowerEligible()` /
`filterRosterByTower()`를 추가하고, 세 진입점(`recommendTeams`, `findExactTeamMatch`,
`findRealUsageTeamMatch`)이 `opts.tower`로 로스터를 걸러 쓰게 했습니다.

```
opts.tower = null | 'elysion' | 'missilis' | 'tetra' | 'pilgrim'
  null      → 일반 트라이브 타워 (제한 없음). 기존 동작 그대로
  pilgrim   → manufacturer === 'pilgrim' || overspec === true
  그 외      → manufacturer === tower   (오버스펙은 소속을 유지하므로 이걸로 이미 커버됨)
```

**"아키타입에 타워 구분이 없다"는 걸림돌은 사라졌습니다.** 아키타입마다 무슨 타워인지
판별하려 했는데, **자격을 캐릭터 쪽에서 판정하고 로스터 하나만 걸러 넣으면** 나머지가 전부
따라옵니다 — 아키타입 후보 조건이 "멤버 전원 보유"라 자격 없는 멤버가 낀 조합은 자동으로
빠지고, `flexSlots`도 걸러진 로스터에서만 채워집니다. 아키타입 데이터를 건드릴 필요가 없어
`synergyNotes.json`은 그대로입니다.

**실측 (전 SSR 168명 보유 가정, 완전일치 1위):**

| 타워 | 출전 가능 | B1/B2/B3 | 완전일치 |
|---|---|---|---|
| 일반 | 168명 | 39/57/72 | Solo Anis: Star |
| 엘리시온 | 43명 | 9/14/20 | El-Macho |
| 미실리스 | 34명 | 5/14/15 | Tia + Naga |
| 테트라 | 51명 | 21/19/11 | Tetra Best Tribe Tower Team |
| 필그림 | 27명 | 5/6/16 | Pilgrims Best Tribe Tower Team |

**오버스펙 플래그가 없으면 필그림 타워는 성립하지 않습니다 (실측):**
오버스펙을 빼면 23명(B1/B2/B3 = 4/6/13)이 되고 **완전일치가 "없음"으로 사라집니다.**
1단계가 없었으면 3단계가 무의미했다는 뜻입니다.

**검증:**
- 무작위 70인 로스터 80건 × 4개 타워 → 완전일치 35건 + 폴백 팀 222건, **자격 위반 0건**
- `tower` 미지정 시 campaign/bossing/pvp/tribe_tower 전부 기존 결과와 **동일**(회귀 없음)
- checkData ERROR 0 / WARN 0 유지

### 4단계에서 배선할 곳

- `app/api/ai-recommend/route.js` — 요청 body에서 `tower`를 받아 `matchOpts`에 넣고
  `recommendTeams`에도 전달 (엔진 호출 3곳은 이미 `opts.tower`를 받습니다)
- **`buildCacheKey`에 `tower`를 넣으세요.** 지금은 멤버·근거가 달라 키가 갈리지만,
  타워만 다르고 결론이 같은 경우가 생기면 설명이 섞일 수 있습니다
- 로스터가 잘려 조합이 안 나올 때의 문구는 엔진이 이미 타워용으로 따로 냅니다
  ("○○ 타워에 출전할 수 있는 버스트 X 캐릭터가 없어…")
- 화면에는 요일제 안내가 필요합니다 — 월 전체 / 화·금 엘리시온 / 수·토 미실리스 /
  목·일 테트라 / 수 필그림

**checkData 추가 검사 (2026-08-08):**
- `OVERSPEC_PILGRIM` / `OVERSPEC_ABNORMAL` — 필그림·abnormal에 플래그가 붙으면 ERROR
- `OVERSPEC_NO_SOURCE` — `overspecNote`에 출처가 없으면 WARN (totemRole과 같은 방식)
- `TOWER_NON_OVERSPEC` — **제조사가 섞인 tribe_tower 조합에 필그림도 오버스펙도 아닌 멤버가
  있으면 ERROR.** 새 오버스펙이 나왔는데 플래그를 빠뜨리면 여기서 잡힙니다.
  역테스트로 실제 발화 확인함(미하라 플래그 제거 → ERROR 1건)
- `TOWER_MIXED_ARCH` — 필그림 없이 제조사만 섞인 tribe_tower 조합이 생기면 ERROR

참고: `abnormal` 제조사 25명은 콜라보·특수 캐릭터로 **제조사 타워가 없습니다.** 일반 타워 전용.

### 검토했으나 착수 안 한 것

| 항목 | 난이도 | 메모 |
|---|---|---|
| PvP 루키/스페셜 분리 | 중 | `metaStats.usageTier.arena`(34명), `pvp.topTeams`(11건) 이미 있음 |
| 챔피언 아레나 3팀 편성 | 큼 | **15명을 겹치지 않게 3팀 배분.** 엔진에 새 로직 필요. 자유 슬롯 탐색 코드를 확장 가능 |
| 요격전 / 시뮬레이션 룸 | 큼 | **데이터 거의 없음.** 티어가 story/bossing/pvp 3개뿐이라 근사 불가. 시뮬레이션은 런마다 버프가 달라 "최적 조합" 개념 자체가 약함 |
| 증폭 니케 우선순위 | ? | **유저에게 '증폭'이 뭔지 확인 필요.** 데이터에 언급 0회. 조합 추천이 아니라 육성 우선순위 성격 |

### 보안 — `ai_recommend_feedback` RLS 활성화 (2026-08-09 완료)

Supabase 어드바이저가 critical로 잡았던 항목입니다. 이 테이블만 RLS가 꺼져 있어
**anon 키를 가진 누구나 전체 행을 읽고 수정·삭제할 수 있었습니다.** anon 키는 프론트 번들에
그대로 들어가므로 사실상 공개 상태였습니다.

`supabase/ai_recommend_feedback_rls_migration.sql` — **2026-08-09 실행 완료.**
**insert 정책만** 열었습니다. 이 테이블을 쓰는 코드가 피드백 라우트의 insert 하나뿐이기
때문입니다(전 코드 grep 확인). 분석은 Supabase 대시보드나 service role로 하면 되고,
그쪽은 RLS를 우회합니다.

**anon 역할로 4가지 동작을 실제로 시험했습니다:**

| 동작 | 결과 | 판정 |
|---|---|---|
| INSERT | 성공 | 피드백 기능 유지 ✅ |
| SELECT | 0건 | 차단 ✅ |
| UPDATE | 반영 안 됨 | 차단 ✅ |
| DELETE | 삭제 안 됨 | 차단 ✅ |

⚠️ **나중에 이 테이블을 읽는 기능을 만든다면** anon 키로는 안 읽힙니다. 정책이 없으면
에러가 아니라 **빈 결과**가 돌아와 조용히 실패합니다(§2-3 유형). service role 키를 쓰거나
select 정책을 따로 만드세요.

**같이 발견한 것**: 피드백 라우트 주석에 "이 데이터가 ai-recommend에서 힌트로 다시 읽힌다"고
적혀 있었으나 **사실이 아니었습니다.** 읽는 코드가 한 곳도 없습니다. 주석을 정정했습니다.

## 7-2. 게시판 — 수정·삭제 + 비밀글 (2026-08-09)

### 수정·삭제가 "안 되던" 진짜 이유

유저 제보: "게시판이랑 조합 올리는 곳이 수정·삭제가 안 된다."
**권한 문제가 아니었습니다.** DB 정책(`posts_update_own` / `posts_delete_own` /
`combos_update_own` / `combos_delete_own`)은 처음부터 `auth.uid() = user_id` 조건으로
정상 설정돼 있었고, **`lib/board.js`·`lib/combos.js`에 함수가, 화면에 버튼이 없었을 뿐**입니다.
같은 착각을 반복하지 않으려면 **정책을 먼저 의심하지 말고 코드에 함수가 있는지부터 보세요.**

추가한 것: `updatePost` / `deletePost` / `updateComment` / `deleteComment` /
`updateCombo` / `deleteCombo`. 화면에는 글 상세·댓글·조합 카드에 본인 것만 수정/삭제 버튼.

⚠️ **자식 레코드를 앱에서 먼저 지우지 마세요.** `comments_post_id_fkey`와
`combo_votes_combo_id_fkey`는 **이미 `ON DELETE CASCADE`** 입니다(2026-08-09 확인).
앱에서 먼저 지우려 하면 `comments_delete_own` / `votes_delete_own` 정책 때문에 **내 것만**
지워지고 남의 것은 남아, 조합은 외래키 위반으로 삭제가 실패하고 글은 "지웠는데 댓글이 남는"
상태가 됩니다. 캐스케이드는 RLS를 거치지 않아 남의 것까지 정상 정리됩니다.
실측: 글 소유자가 삭제 → 남이 쓴 댓글까지 함께 삭제됨.

### 비밀글

`supabase/board_private_posts_migration.sql` — **2026-08-09 실행 완료.**

- `posts.is_private` 추가. **글 단위 선택**이고 버그 제보만 기본값 true
- **버그 게시판 전체를 무조건 비밀로 하지 않은 이유**: (1) 중복 제보가 늘고 (2) "알고 있다"는
  신호를 줄 수 없으며 (3) 색인 페이지가 줄어 **애드센스 심사에 불리**합니다(§9의 "콘텐츠가
  얇다" 위험과 같은 방향). 지금 사이트 검토 대기 중이라 방향이 반대입니다
- 남의 비밀글은 **목록에도 안 나옵니다.** RLS 하나로 끝나 새는 경로가 없습니다
- **댓글도 함께 막았습니다.** 이걸 빠뜨리면 post_id만 알면 비밀글의 댓글을 읽을 수 있습니다 —
  본문은 막고 옆문이 열려 있는 셈이라 비밀글 기능에서 가장 흔한 구멍입니다
- 운영자: `profiles.is_admin` + `is_board_admin()`(security definer).
  현재 운영자는 `jyd7661@gmail.com`. 추가하려면 그 사람의 `profiles.is_admin`을 true로

⚠️ **권한 상승을 한 번 뚫렸습니다(고침).** `profiles_update_own` 정책이 있어 사용자가 자기
행을 수정할 수 있는데, 처음에 `revoke update (is_admin)`으로 **컬럼 단위만** 회수했더니
효과가 없었습니다 — **Postgres에서는 테이블 단위 UPDATE 권한이 남아 있으면 컬럼 단위 회수가
무시됩니다.** 테스트 계정이 실제로 자기 `is_admin`을 true로 만드는 데 성공했습니다.
`revoke update on profiles` → `grant update (nickname)` 으로 고쳤습니다.

**검증(anon/authenticated 역할로 실측):**

| 시나리오 | 보이는 글 | 비밀글 | 비밀글 댓글 |
|---|---|---|---|
| 제3자 | 공개글만 | 0 | 0 |
| 작성자 본인 | 전부 | 1 | 1 |
| 운영자 | 전부 | 1 | 1 |

일반 사용자의 `is_admin` 변경 → 반영 안 됨 / 닉네임 변경 → 정상.

### 서킷 브레이커의 남은 위험 (2026-08-09 확인)

RLS를 고친 뒤 Supabase 어드바이저에 **새 WARN이 하나 생겼습니다** —
`increment_ai_daily_budget()`을 anon이 실행할 수 있다는 것. **이건 의도한 설계입니다**
(라우트가 anon 키로 호출해야 함). 다만 잔여 위험이 있습니다:

**누군가 `/rest/v1/rpc/increment_ai_daily_budget`을 직접 반복 호출해 카운터를 부풀리면
그날의 AI 설명 생성이 조기에 멈춥니다.** IP당 30회 제한은 이 경로를 거치지 않습니다.

**그래도 이 방향이 낫다고 판단한 이유**: 반대로 update를 열어두면 카운터를 0으로 만들어
**상한 자체를 무력화**할 수 있고 그건 곧 비용입니다. 지금 구조에서 가능한 악용은
"부풀리기"뿐이고, 그 결과는 **비용이 아니라 가용성**(설명이 담백해짐)입니다. 게다가 상한에
걸려도 조합·점수·근거는 그대로 나갑니다. 나쁜 쪽으로 실패하게 두는 편을 택했습니다.

**제대로 막으려면**: Vercel에 `SUPABASE_SERVICE_ROLE_KEY`를 넣고 이 RPC만 service role로
호출한 뒤, anon의 EXECUTE 권한을 회수하면 됩니다. 비밀키를 하나 더 두는 결정이라 보류했습니다.

### 별건 — `combo_scores` 뷰가 SECURITY DEFINER (미해결)

같은 어드바이저에서 **ERROR** 등급으로 잡힙니다. 뷰가 조회자가 아니라 **생성자의 권한과 RLS로**
동작해서, 뷰를 통하면 원래 못 볼 데이터가 보일 수 있습니다. 조합 투표 집계 뷰라 민감도는
낮아 보이지만 확인이 필요합니다. 이번 작업 범위 밖이라 손대지 않았습니다.

### 남은 개선 사항

- **`limited` 태그 미사용** — 미보유 캐릭터 안내에 활용 가능
- **낭비 판정 부분 점수** — 0/전부가 너무 거칠지만 근거 있는 수치가 없어 보류
- **콘텐츠 보강** — 애드센스 검토에 유리. 지금은 도구 성격이 강해 "콘텐츠가 얇다" 판정 위험
- **빈칸 조합의 구조적 유리함** — 로스터에서 최적으로 채워지므로 5인 확정 조합보다 유리.
  전 캐릭터 보유 가정 시 상위 1~3위가 전부 빈칸 4개짜리. 나중에 "확정 조합만 보기" 필터가 필요할 수 있음

---

## 7-3. 커뮤니티 번역 — 용어집 (2026-08-09)

목표: 한국어로 쓴 글을 일본어 설정 사용자가 일본어로 읽는 것. 사용자 요구는
**"번역이 완벽해야 하고, 니케 이름이나 그 외의 것들 모두 번역이 이루어져야 한다"** 입니다.

### 왜 용어집이 먼저인가

AI에게 이름을 맡기면 매번 다르게 나옵니다. 그래서 **AI가 이름을 건드릴 여지를 0으로 만드는**
치환 방식을 씁니다:

```
원문 → 알려진 이름/용어를 토큰(⟦C7⟧)으로 치환 → 번역 → 대상 언어 표기로 되돌림
```

이 방식이 성립하려면 세 언어 표기가 전부 있어야 합니다. 그래서 용어집이 선행 작업입니다.

### 캐릭터 이름 — `characterDatabase.json`의 `title` / `name_kr` / `name_ja`

`name_ja`를 **196명 전원** 추가했습니다(2026-08-09). 별도 파일이 아니라 캐릭터 DB에 둡니다 —
이름은 캐릭터의 속성이지 용어가 아니고, 파일이 갈리면 한쪽만 갱신되는 사고가 납니다.

**출처와 수집 방법 (재현 가능하도록 기록):**

주 출처는 `wiki3.jp/nikke/page/269`(부대별 분류)입니다. 이 페이지 하나가 전 로스터를
**부대 단위로 묶어서, 정식 표기(`기본명：코스튬`)로** 제공합니다. 우리 DB에도 `squad` 필드가
있어 그룹 자체가 대조 근거가 됩니다.

**⚠️ 정정 (2026-08-10):** 제가 2026-08-09에 "한 곳에서 한/영/일을 다 주는 출처는 없다"고
적었는데 **틀렸습니다.** 주간 예약 작업이 찾아냈습니다:

> **`https://wiki3.jp/nikke/page/236` — 海外名(해외명) 페이지. JP ↔ EN ↔ KR 매핑 128행.**

제가 검색으로 못 찾고 없다고 단정한 겁니다. 부대별 페이지(269)로 짝을 맞춘 결과 자체는
이 페이지와 대조해도 **name_ja 128명 전원 글자 그대로 일치**해서 데이터는 무사했지만,
**출처 조사를 "없다"로 끝내면 안 됐습니다.** 앞으로 이름·표기 작업은 236을 먼저 보세요.

한계: 236은 **128행뿐이라 로스터 196명보다 68명 적습니다**(코스튬 변형·최근 신규 미수록).
그 68명은 여전히 269 + 개별 페이지로 대조해야 합니다.

**236 전수 대조 결과 (2026-08-10, 128행 전부 스크립트 대조):**

| 항목 | 결과 |
|---|---|
| `name_ja` ↔ 236 | **128/128 일치, 불일치 0** — 부대별(269)로 맞춘 짝이 독립 출처로 재확인됨 |
| `name_kr` ↔ 236 | 불일치 **1건** — `D`: 우리 `D(디)` vs 236 `D` |
| `title`(영문) ↔ 236 | 불일치 **9건** (아래) |

영문 9건은 성격이 다릅니다:
- **EVA 콜라보 4명 — 어순.** 236/에반게리온 정본은 `Shikinami Asuka Langley`(성-이름),
  우리는 `Asuka Shikinami Langley`(이름-성). **다만 게임 자체(prydwen 기준)가 우리 쪽 어순을
  씁니다.** `綾波レイ`의 236 표기 `Ayanami Rai`는 위키 쪽 오기로 보입니다(정본 Rei).
- **`iDoll` 3명 — 대소문자.** 우리 `IDoll` vs 236 `iDoll`.
- **`Helm: Aqua Marine`(띄어쓰기) / `Ludmilla Winter Owner`(콜론 없음)** — 236 쪽 표기 흔들림.

⚠️ **`title`은 표시용 이름이 아니라 파일 간 조인 키입니다.** characterDatabase ↔ synergyNotes ↔
characterInvestmentNotes ↔ metaStats가 전부 이 문자열로 연결되고 checkData가 일치를 강제합니다.
바꾸려면 네 파일을 함께 고쳐야 하고, **주 출처(prydwen)를 다시 긁으면 원래 표기가 되돌아옵니다.**

### name_ja 이중검증 현황 (2026-08-10)

`name_ja` 196명의 1차 출처는 **부대별 분류(page/269)** 입니다. 여기에 독립 출처가 하나라도
더 붙는지를 전수 계산했습니다.

| | 결과 |
|---|---|
| **2개 이상 출처로 확인** | **174 / 196 (89%)** |
| 부대별(269) 단독 | 22명 |

대조에 쓴 출처: 해외명(236) · 일람(14) · 코스튬(2251) · 속성별(2947) · 클래스별(778).
표기 흔들림(`：` vs 공백 vs `・`)은 정규화해서 비교했습니다.

**남은 22명은 더 파도 안 나옵니다 — 이유가 구조적입니다.**
wiki3의 목록 페이지들은 **최근 캐릭터를 일본어 텍스트가 아니라 영문 이미지(alt)로 렌더링**합니다.
그래서 목록 페이지를 아무리 더 가져와도 그 22명은 계속 영문으로만 잡힙니다. 실제로 확인:
속성별·클래스별 페이지에서 22명 **전원이 영문으로 표시**됩니다.

22명 목록 (전부 2024-10 이후 출시 + 콜라보):
`Rumani` `Grave` `Ark Ranger Black` `Rapunzel: Pure Grace` `Asuka: WILLE`
`Rei Ayanami (Tentative Name)` `Sakura Suzuhara` `Avistar` `Bready` `Crust` `Prika`
`EVE` `Raven` `Dorothy: Serendipity` `Ade: Agent Bunny` `Milk: Blooming Bunny`
`Ada Wong` `Jill Valentine` `Claire Redfield` `Snow White: Heavy Arms` `Anis: Star`
`Cinderella: Crystal Wave`

**이 22명을 닫으려면 캐릭터별 개별 페이지 22번 조회가 필요합니다.** 지금은 안 했습니다 —
부대별 대조에서 부대 그룹까지 일치했고 목록 페이지들이 영문으로나마 같은 캐릭터의 존재를
확인해주므로, 비용 대비 실익이 낮다고 판단했습니다. 필요해지면 그때 하세요.

⚠️ **신규 캐릭터는 처음엔 항상 이 22명과 같은 상태로 들어옵니다**(목록 페이지가 영문 이미지로만
싣기 때문). 주간 예약 작업이 신규 name_ja를 넣을 때 "교차검증 2건 통과"를 요구하는데,
**신규는 그 조건을 못 채우는 게 정상**입니다. 부대별에서 부대 그룹이 맞으면 통과로 봅니다.

### 결정 (2026-08-10) — 영문 9건은 **고치지 않습니다**

**`title`은 게임(NIKKE 글로벌) 표기를 따릅니다.** 유저 결정: *"미국은 글로벌 기준으로 가는 게 맞겠네"*.

근거: 영어권 유저는 게임에서 본 이름으로 검색합니다. 우리만 다른 어순을 쓰면 못 찾습니다.
그리고 이 필드는 조인 키라 바꾸면 네 파일을 함께 고쳐야 하고 다음 스크랩 때 되돌아갑니다.

⚠️ **에반게리온 정본은 `Shikinami Asuka Langley`(성-이름)가 맞습니다** — 유저가 에반게리온
정보 사이트 자료로 확인해줬습니다. 그런데도 게임 표기를 따르기로 한 것이니, 나중에 누가
"정본과 다르다"며 되돌리지 마세요.

**이 결정이 일본·한국 유저에게는 아무 영향이 없습니다.** 그쪽은 `name_ja`(`式波・アスカ・ラングレー`)와
`name_kr`(`시키나미 아스카 랑그레이`)를 보고, 둘 다 이미 정본이며 236 대조에서도 일치했습니다.
처음에 제가 이 항목을 "표기 정책"이라고만 설명해서 유저가 시장 규모를 근거로 판단할 뻔했는데,
**영문 title은 영어권 유저에게만 보입니다.** 다음에 같은 논의가 나오면 이 점을 먼저 밝히세요.

나머지 5건(`Ayanami Rai` / `Helm: Aqua Marine` / `Ludmilla Winter Owner` / `iDoll` 3건 대소문자)은
**236 쪽 표기 흔들림**이고 우리 표기가 맞습니다.

### 결정 (2026-08-10) — `D`의 한글 표기는 `D`로 정정 (반영 완료)

236의 한국어 칸이 `D`입니다. 기존 `D(디)`는 출처 불명이라 `D`로 고쳤습니다
(`characterDatabase.json`, `characters.js` 둘 다).

**같이 한 조치:** 한글 이름이 알파벳 한 글자가 되면 한국어 문장 속 아무 `D`나 캐릭터로 잡힙니다.
그래서 `AMBIGUOUS_KO`에 `D`를 넣어 기계 치환에서 빼고 AI가 문맥으로 판단하게 했습니다.
유저 관찰이 근거입니다 — *"D를 니케로 말하려면 보통 D만 따로 띄어쓴다"*.
`scripts/testGlossary.mjs`에 회귀 테스트 3건 추가(`D랭크 보상`은 미치환, `D : 킬러 와이프`는 정상 치환).

당시 확인해서 **실제로 안 되던** 출처들(이건 유효):

| 출처 | 결과 |
|---|---|
| 일본 공식 사이트 | 캐릭터 목록 페이지 없음 |
| Fandom 국제 위키 | 봇 차단(402) |
| 나무위키 / 인벤 | 한글만 |
| game8 / gamewith / kamigame | 일본어만, 별칭 표기 혼용(`ネオン(オバスペ)`) |

일본판 이름은 영문명의 가타카나 전사이고 구조가 같습니다
(`Rapi: Red Hood` → `ラピ：レッドフード`). 하지만 **전사 규칙만으로는 안 됩니다** —
`Frima`가 `プリム`인 것처럼 어긋나는 사례가 있습니다. 그래서 전사로 추측하지 않고
**출처 문자열을 그대로 가져와 짝만 맞추는** 방식을 썼습니다.

**검증 결과 (전부 스크립트로 확인, 실패 0):**

- 196명 ↔ 출처 196개 **완전 일대일** (미사용 출처 0, 미매칭 캐릭터 0)
- 모든 `name_ja`가 출처에 **글자 그대로** 존재
- 부대 그룹 일치 — DB `squad`의 멤버 집합이 항상 일본 출처의 한 부대 안에 포함
- **독립 교차검증 2건**
  - 속성별 페이지(`page/2947`)와 대조 — 125명 확인, 불일치 0
  - 일람 페이지(`page/14`)의 `【SSR】` 표기와 희귀도 대조 — 86명 확인, 불일치 0

부수 성과: `page/84`(기업별)에 `オーバースペック` 절이 따로 있고 **Rapi / Mihara / Anis / Neon
정확히 4명** — 오버스펙 데이터(§9)가 독립 출처로 재확인됐습니다.

### 게임 용어 — `data/glossary.json`

캐릭터가 **아닌** 용어만 담습니다(기업 4, 속성 5, 클래스 3, 콘텐츠·시스템 8 = 20개).
행마다 `source`가 필수입니다. 출처 없는 표기는 넣지 않습니다(§2-2).

확인된 표기 중 헷갈리기 쉬운 것:

- 애장품 = `Treasure` = **`宝もの`** (`愛用品`이 아님)
- 화력형 = `Attacker` = `火力型` (한국어도 '공격형'이 아니라 **화력형**)
- 수냉 = `Water` = `水冷`

### `checkData.mjs`가 지키는 것 (2026-08-09 추가, 전부 역테스트 완료)

| 코드 | 등급 | 잡는 것 |
|---|---|---|
| `NAME_MISSING` | ERROR | 세 언어 중 하나라도 이름이 빈 캐릭터 |
| `NAME_DUP` | ERROR | 같은 언어 안에서 이름이 겹침 → 되돌릴 때 구분 불가 |
| `NAME_SUBSTRING` | WARN | 짧은 이름이 다른 이름 안에 통째로 들어 있음 |
| `GLOSSARY_INCOMPLETE` | ERROR | 용어에 ko/en/ja/source 중 빈 칸 |
| `GLOSSARY_DUP` | ERROR | 용어집 안에서 같은 언어 표기가 겹침 |
| `GLOSSARY_NAME_COLLISION` | ERROR | 용어 표기가 캐릭터 이름과 같음 |

### 모호어 목록 점검 절차 (게시판 글이 쌓이면)

`lib/glossary.js`의 `AMBIGUOUS_KO/EN/JA`는 **게임 이름이면서 일상 낱말이기도 해서 기계 치환을
하지 않는** 표기 목록입니다. 지금 한국어 13개(신·소다·밀크·은화·이브·홍련·루피·파워·노아·
아리아·크라운·민트·슈가)는 **근거가 제 언어 감각뿐**이라 게임 데이터와 격이 다릅니다.

**⚠️ 비용이 비대칭입니다.** 목록에 있어도 번역이 안 되는 게 아닙니다 — 기계 치환만 안 하고
AI에게는 대조표(`ambiguousHints`)가 그대로 넘어갑니다.

| 잘못 | 결과 |
|---|---|
| 넣었는데 안 겹침 | 이름 번역이 조금 덜 확실 (AI가 대부분 맞춤) |
| 뺐는데 실제로 겹침 | **멀쩡한 문장이 깨짐** |

#### ⚠️ 목록에 있는 13개를 세지 마세요 — 전체 글을 읽으세요

처음에 저는 "13개 단어가 몇 번 나왔는지 세는" 절차를 적었는데, **유저가 방향이 틀렸다고
지적했습니다(2026-08-09):**

> 글자를 정해놓고 개수를 샌다기보다 전체 글을 읽으면서 번역 결과가 뜻이 이상하다면
> 가능성 있는 단어로 하나씩 추가하는 게 맞다고 봐

맞는 지적입니다. 정해둔 목록만 세면 **이미 의심하는 것만 확인**하게 되고, 정작 위험한
"우리가 생각도 못 한 단어"는 영영 못 찾습니다. 그 절차로는 목록에서 **뺄 수만 있고 추가는
절대 못 합니다.** 그런데 실제 사고는 목록에 **없는** 단어에서 납니다.

**후보 범위는 아무 한국어 낱말이 아니라 우리가 아는 216개 표기(캐릭터 196 + 용어 20)입니다.**
치환은 그 안에서만 일어나므로, 사고도 그 안에서만 납니다.

#### 절차

1. 최근 글을 **원문과 번역본을 나란히 놓고 읽는다** (아래 쿼리)
2. 번역된 뜻이 이상한 글을 찾는다 — 개수를 세는 게 아니라 **읽는다**
3. 이상한 글의 `protected` 컬럼을 본다. **그 글에서 어떤 낱말이 이름으로 치환됐는지**가
   그대로 들어 있다. 원인 단어가 대개 여기서 바로 보인다
4. 일상 뜻으로 쓰인 낱말이 이름으로 치환됐다면 → 그 표기를 `AMBIGUOUS_*`에 **추가**한다
5. 목록에 있는데 실제 글에서 **10건 이상 전부 니케 얘기로만** 쓰였다면 제거를 검토한다.
   한 건이라도 일상 뜻이면 그대로 둔다
6. 목록을 고쳤으면 `scripts/testGlossary.mjs`에 그 단어의 회귀 테스트를 추가하고 돌린다

```sql
-- 원문 + 번역본 + 치환된 낱말을 한 줄로 (읽기용)
select p.id, p.source_lang, p.title, p.content,
       t.lang, t.fields->>'title' as title_tr, t.fields->>'content' as content_tr,
       t.status, t.protected
from posts p
left join content_translations t
       on t.entity_type = 'post' and t.entity_id = p.id
order by p.created_at desc
limit 30;
```

`protected` 예시 — 이 글에서 무엇이 이름으로 바뀌었는지:
`[{"id":"c:rapi","form":"라피"}, {"id":"t:corp_elysion","form":"엘리시온"}]`

**보조 지표**(주 지표 아님). 어떤 표기가 글에 자주 등장하는지만 알려줍니다.
자주 나오는 것과 겹치는 것은 다른 얘기이므로 이 숫자만으로는 아무것도 정하지 마세요:

```sql
select t.form, count(*) 치환건수
from content_translations c, jsonb_to_recordset(c.protected) as t(id text, form text)
group by t.form order by 치환건수 desc;
```

**⚠️ 목록을 고치면 `glossaryVersion()` 값이 바뀝니다.** 이미 저장된 번역본은 예전 기준으로
만들어진 것이라 다시 번역해야 최신 표기가 반영됩니다(`content_translations.glossary_ver`로
대상을 찾을 수 있습니다).

**⚠️ 목록 수정은 자동으로 하지 않습니다.** 잘못 빼면 멀쩡한 문장이 깨지고, 잘못 넣으면
이름 번역이 흐려집니다. 항상 사람이 확인합니다.

### ⚠️ 치환 구현 시 반드시 지킬 것

`NAME_SUBSTRING` 경고가 알려주는 실제 위험입니다:

- 한글 18개 — `신`(→`신데렐라`), `이브`(→`그레이브`), `헬름`(→`헬름 : 아쿠아마린`) 등
- 일본어 8개 — `シン`(→`シンデレラ`), `D`(→`I-DOLL・サン`), `紅蓮`(→`紅蓮：ブラックシャドウ`) 등
- 영문 2개 — `D`, `K`

**치환은 반드시 긴 이름부터** 하고, `D`·`K`처럼 일반 문장에도 나올 수 있는 이름은
앞뒤 경계까지 확인해야 합니다. 이걸 빼먹으면 "신데렐라"가 "シンデレラ"가 아니라
"シン데렐라"가 됩니다.

### ✅ 타워 요일 정정 (2026-08-09) — prydwen이 틀렸습니다

번역 작업 중 일본 위키와 대조하다 **화면에 잘못된 요일이 떠 있었다는 걸 발견했습니다.**

옛 값은 prydwen 가이드를 그대로 옮긴 **"월요일에 전부 열림"** 이었는데,
`wiki3.jp/nikke/page/992`는 일요일을 공통 요일로 적고 있어 어긋났습니다.
한국 서버를 직접 하는 유저가 **"일요일에 모든 타워가 열린다"** 고 확인해줬고,
그 기준으로 두 출처가 정확히 맞아떨어집니다.

| 기업 | 현재(정정 후) | 옛 값(틀림) |
|---|---|---|
| 엘리시온 | 화·금·**일** | 월·화·금 |
| 미실리스 | 수·토·**일** | 월·수·토 |
| 테트라 | **월**·목·일 | 월·목·일 |
| 필그림/오버스펙 | 수·**일** | 월·수 |

**남은 불확실성:** 테트라의 **월요일은 일본 위키 한 곳만 근거**입니다. 옛 데이터는 월요일을
전체 개방일로 착각하고 있어서 테트라 단독 요일로 세지 않았습니다. 나머지 칸은 두 출처가
독립적으로 일치합니다.

**교훈:** prydwen은 이 프로젝트의 주 출처지만 **요일·일정처럼 서버 운영에 딸린 정보는 틀릴 수
있습니다.** 유저가 실제로 플레이하는 한국 서버가 최종 기준입니다.

### 저장 구조 — `content_translations` (2026-08-09 적용 완료)

`supabase/content_translations_migration.sql`. 적용 시점에 posts/comments/user_combos가
모두 0행이라 백필 대상은 없었습니다.

- `posts` / `comments` / `user_combos` 에 **`source_lang`** 추가 (기본값 `'ko'`).
  원문이 무슨 언어인지 모르면 무엇을 번역하고 무엇을 '원문'으로 보여줄지 정할 수 없습니다.
- **`content_translations`** — PK `(entity_type, entity_id, lang)`, 번역된 필드는 `fields` jsonb.
  각 테이블에 `title_ja`, `content_ja` 식으로 컬럼을 붙이지 않은 이유는
  3테이블 × 최대 3필드 × 대상 언어라 컬럼이 금방 18개가 되고, 언어를 하나 늘릴 때마다
  세 테이블을 다 고쳐야 하기 때문입니다. 지금 구조에서는 **언어 추가가 데이터 추가로 끝납니다.**
- `glossary_ver` — 용어집이 바뀌면 예전 번역은 낡은 표기를 씁니다. 다시 번역할 대상을
  찾기 위한 값입니다.

**⚠️ 비밀글이 번역본으로 새지 않게 하는 부분이 이 설계의 핵심입니다.**
select 정책이 원본 테이블을 참조하도록 위임합니다 —
정책식 안에서 다른 테이블을 참조하면 **그 테이블의 RLS도 함께 적용**되므로,
`exists (select 1 from posts where id = entity_id)` 는 호출자가 원본을 볼 수 있을 때만 참입니다.
덤으로 원본이 지워진 고아 행도 자동으로 안 보이게 됩니다.

**쓰기는 서버(service role)만.** §7-4를 겪은 뒤라 처음부터 닫고 시작했습니다.

**실제로 돌려본 검증 3건 (전부 통과, 트랜잭션 rollback):**

| 검증 | 결과 |
|---|---|
| 비밀글 번역본 노출 | anon·타인 = 공개글만 / 작성자 = 둘 다 ✅ |
| 클라이언트가 번역본 심기 | `permission denied for table content_translations` ✅ |
| 원본 삭제 시 번역본 정리 | 트리거로 0건 남음 ✅ |

`checkData.mjs`에 `LOCALE_DRIFT` 추가 — `lib/i18n.js`의 `LOCALES`와
`content_translations.lang` 제약이 어긋나면 ERROR. 한쪽만 고치면 "에러 없이 그냥 번역이
안 되는" 상태가 되는데, 그게 §2-3이 말하는 조용한 누락입니다. 역테스트 완료.

---

## 7-4. 🔴 보안 — 서버가 공개 키로 DB에 쓰고 있었습니다 (2026-08-09)

### 무슨 일이었나

`app/api/ai-recommend/route.js`가 **`NEXT_PUBLIC_SUPABASE_ANON_KEY`로 DB에 쓰고 있었습니다.**
서버가 공개 키를 쓰니 서버가 써야 하는 테이블을 전부 `anon`에게 열어줄 수밖에 없었고,
구멍 세 개가 전부 여기서 나왔습니다.

| 대상 | 가능했던 일 | 등급 |
|---|---|---|
| `ai_explain_cache` | **캐시된 AI 설명문을 아무나 바꿔 씀.** 그 글은 화면에 나가는 추천 근거이고 캐시라 계속 재사용됨 → 내용 변조 | 🔴 |
| `ai_explain_usage` | 남의 사용량을 최대로 올려 차단 / 자기 것만 되돌려 무제한 사용 | 🟠 |
| `increment_ai_daily_budget()` | 인증 검사 없이 아무나 호출 → 하루 상한을 몇 초 만에 태워 AI 설명 전면 차단 | 🟠 |

**RLS 정책과 테이블 GRANT 두 겹이 모두 열려 있는지 반드시 같이 확인해야 합니다.**
정책만 보고 판단하면 틀립니다(반대 방향 실수를 §7-2 `profiles`에서 이미 한 번 했습니다).

### 완료된 것

- `ai_explain_cache`: 테이블 UPDATE 회수 후 `(hits, last_hit_at)`만 재부여
  → **`reasoning` 변조 차단.** 배포 없이 즉시 적용됨
  (마이그레이션 `lock_ai_explain_cache_reasoning_column`)
- `combo_scores` 뷰: `security_invoker = on` (advisor ERROR 해소, 동작 변화 없음)
- `route.js`: service role 키를 쓰도록 수정. **키가 없으면 공개 키로 되돌아가지 않고
  Supabase 연동 자체를 끕니다** — 되돌아가면 "고쳤다고 생각했는데 옛날 상태"가 되고
  그게 §2-3의 조용한 누락입니다. 대신 `[AI_KEY]` 태그로 에러 로그를 남깁니다

### 남은 순서 (이 순서를 지키지 않으면 배포된 사이트가 조용히 깨집니다)

1. Vercel에 `SUPABASE_SERVICE_ROLE_KEY` 추가 — **`NEXT_PUBLIC_` 붙이면 번들에 실려 나갑니다**
2. `route.js` 배포
3. 사이트에서 AI 추천 1회 실행 → 캐시·카운터 기록 확인
4. `supabase/ai_tables_lock_anon_write_migration.sql` 적용 (파일 안에 확인 쿼리까지 있음)

### ✅ 익명 로그인 — 껐습니다 (2026-08-09)

켜져 있었고, 익명 계정 2개는 **전부 배당앱 것**이었습니다(`div_holdings` 5건씩, 니케 데이터 0).
니케 사이트에는 `signInAnonymously`를 부르는 코드가 **한 줄도 없어서** 얻는 것 없이 공격면만
남은 상태였습니다 — 켜져 있으면 누구나 무제한으로 인증 계정을 만들어 게시판 도배와 조합
추천수 조작을 할 수 있습니다.

MCP로는 못 바꿉니다. **Chrome 확장(`mcp__claude-in-chrome__*`)으로 대시보드를 직접 조작**해서
껐고, 새로고침 후 상태가 유지되는 것까지 확인했습니다
(가입허용 ON / 수동연결 OFF / **익명 OFF** / 이메일확인 ON).
그 결과 advisor의 `auth_allow_anonymous_sign_ins` 경고 13건이 한꺼번에 사라졌습니다.

> 참고: `computer_*`(기기 제어) 도구는 **브라우저를 읽기 전용으로만** 허용합니다.
> 웹에서 클릭·입력이 필요하면 Chrome 확장 쪽을 쓰세요.
>
> 그리고 이때 `device_bash`(사용자 컴퓨터의 리눅스 작업공간)가 `Workspace unavailable`로
> 죽어 있어서 `cowork-lock.sh`를 실행할 수 없었습니다. 잠금 파일을 직접 읽고 쓰는 방식으로
> 스크립트와 같은 순서(대기표 등록 → 잠금 확인 → 획득 → 검증)를 따랐습니다.
> **다시 이런 상황이 오면 같은 방법을 쓰되, 다른 세션이 `waiting` 상태인지 반드시 먼저
> 확인하세요.** 확인 없이 잠금 파일을 덮어쓰면 남의 잠금을 지우게 됩니다.

### ⚠️ `device_bash`가 `Workspace unavailable`일 때 — 재시작 후 **몇 분** 기다릴 것

2026-08-09에 겪었습니다. 증상은 `device_bash`만 죽고 파일 작업(`device_stage_files` /
`device_commit_files`)·폴더 연결·Chrome 확장은 전부 정상입니다.

**해결은 Claude 데스크톱 앱 완전 종료 후 재시작인데, 리눅스 VM이 뜨는 데 시간이 걸립니다.**
저는 재시작 직후·45초 후·2분 30초 후 세 번 시도해서 전부 실패하는 걸 보고
**"재시작으로 해결되는 종류가 아니다"라고 잘못 결론지었습니다.** 실제로는 약 6분 뒤에
정상 복구됐습니다.

오류 메시지가 `still starting`이 아니라 `failed to start`라서 부팅 중이 아니라고 판단했는데,
**그 메시지는 부팅이 아직 안 끝난 상태에서도 나옵니다.** 재시작했다면 최소 5~10분은
기다렸다가 다시 확인하고, 그 전에 WSL/BIOS/디스크 같은 걸 파헤치지 마세요.

### 안 고친 것과 그 이유

- `app/api/ai-recommend/feedback/route.js`는 아직 공개 키로 `ai_recommend_feedback`에
  INSERT합니다. 이 테이블은 insert 전용 정책이라 읽히지 않고, 피드백은 원래 누구나 보내는
  것이라 위험이 아닙니다. 도배는 가능하니 나중에 같이 옮기면 됩니다.
- `is_board_admin()`은 `anon` 실행 가능하지만 호출자 자신의 `is_admin`만 돌려주고
  익명은 `auth.uid()`가 null이라 항상 false입니다. 그대로 둡니다.

---

## 10. 유저에 대해 알아둘 것

- **게임 지식이 깊습니다.** 크크마 로테이션, 프리카+민트 픽률, 오버스펙 규칙 등 여러 차례
  데이터의 오류를 게임 지식으로 잡아냈습니다. 유저 지적은 대체로 맞으니 진지하게 검증하세요.
- **근거를 중시합니다.** "왜 이렇게 됐는지"를 자주 묻습니다. 추측으로 답하지 말고
  실제로 재현·측정해서 답하세요. 이 프로젝트의 거의 모든 결정이 실측 뒤에 내려졌습니다.
- **틀렸으면 인정하고 고치는 것을 선호합니다.** 실제로 여러 번 제 판단이 틀렸고(크라운 제외
  근거, 마스트 토템 설명, 이미지 파일 한쪽만 수정), 바로잡은 기록이 코드 주석에 남아 있습니다.
- 응답은 **간결하게**. 불필요한 설명을 싫어합니다.

---

## 11. 새 세션 시작 프롬프트 예시

```
니케 조합 추천 사이트 작업을 이어서 합니다.
로컬 클론: C:\Users\정연도\Desktop\nikke-site-git\
먼저 HANDOFF.md를 읽고, checkData.mjs와 findTotems.mjs를 돌려 현재 상태를 확인해주세요.
그다음 "기업 타워 모드 추가"의 1단계(오버스펙 니케 목록 수집)부터 진행합니다.
```
