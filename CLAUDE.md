# 니케 조합 추천 사이트

모바일 게임 **승리의 여신: 니케**의 조합 추천 사이트. 보유 니케를 고르면 용도(캠페인·보스전·
PvP·타워)에 맞는 5인 조합을 추천하고, 커뮤니티에서 조합·정보를 나눌 수 있다.

- 코드: `nikke-site/` (Next.js 14 App Router · Tailwind · Vercel 배포)
- 배포: https://nikke-site.vercel.app · DB/인증: Supabase (`yttfwroeyplwrchyitud`)
- 운영자 겸 기획자 1인. 사이트가 번영하도록 데이터 관리·수집·유지보수·고객응대를 함께 한다.

## 세션 시작 시 먼저 할 일

```bash
cd nikke-site
node scripts/checkData.mjs           # 기준선: ERROR 0 / WARN 3
node scripts/testI18n.mjs            # 22건
node scripts/testCharacterNames.mjs  # 26건
node scripts/testGlossary.mjs        # 35건
node scripts/findTotems.mjs          # 기준선: 1군 0명
```

이 스크립트들이 이 프로젝트의 안전망이다. **뭔가 고친 뒤에는 반드시 다시 돌린다.**
`checkData`가 ERROR를 내면 그 변경은 되돌리는 것이 원칙이다.

> WARN 3건은 `name_ja` 도입으로 생긴 `NAME_SUBSTRING` 경고이며 정상이다.

---

## 핵심 설계 원칙 — 이걸 먼저 이해할 것

### 1. AI는 조합을 만들지 않는다

**조합 구성·점수·근거는 100% `lib/synergyEngine.js`가 결정한다.** AI는 이미 확정된 5명을
자연스러운 한 문단으로 설명하는 일만 한다.

여러 차례 시행착오 끝에 확정된 구조다. 예전에는 AI에게 자유 구성을 맡겼는데 "왜 이 조합이
나왔는지" 설명이 계속 부실했고 사용자가 반복해서 지적했다. **절대 AI에게 조합 구성을 돌려주지
말 것.** 이 구조 덕분에 엔진이 완전히 결정적(deterministic)이고, 그래서 캐싱이 가능하다.

### 2. 근거 없는 숫자를 만들지 않는다

"가중치를 줘서 해결하자"를 여러 번 시도했다가 **전부 실측으로 기각**했다:

- 스킬 시너지에 가중치 → 티어만 깎이고 검증 가능한 시너지는 0건 증가. 기각
- 파트너 조건을 티어보다 우선 → 경고 하나 없애려고 평균 7점 손실. 기각
- 낭비 인원 부분 점수(70%) → 근거 없는 수치라 미채택

대신 **조건을 밝히고 판단은 사용자에게 넘긴다.** `invest`/`expert`/`partner` 경고가 전부 이 방식.

### 3. "조용한 누락"이 가장 위험하다

실제로 일어난 사고들 — 전부 **에러 없이 조용히 잘못된 결과**를 냈다:

- `name_kr`이 `"{{hover"`(위키 템플릿 잔재)여서 **홍련(PvP SS)이 모든 추천에서 누락**
- prydwen 조합의 빈 칸을 스크랩 때 버려서 **5인 팀이 2~4인으로 잘려 후보에서 제외**
- 마스트: 로망틱 메이드가 토템 목록에 없어 **프리카+민트 조합이 한 번도 추천되지 않음**
- `AiRecommendSection`이 `useLanguage()` 훅을 안 받아 **AI 추천 섹션이 통째로 렌더 안 됨**

**새 데이터·새 기능을 넣을 때는 검사기를 함께 확장한다.** 그리고 **검사를 만들면 반드시
역테스트한다** — 일부러 고장을 넣어 그 검사가 실제로 잡는지 확인할 것.

### 4. 검사의 판정 단위 = 고장의 단위

`testI18n`을 **파일 단위**로 짰다가 컴포넌트 스코프 버그를 놓쳤다. 한 파일에 컴포넌트가
여럿이면 스코프가 각각인데 파일 어딘가에 훅이 하나만 있어도 통과했다. 컴포넌트 단위로
고치자 같은 버그가 하나 더 나왔다.

> 검사가 있다는 사실이 검증됐다는 뜻은 아니다. **오탐이 나는 검사는 아무도 믿지 않는다.**

---

## 주제별 문서 (`docs/`)

필요할 때 읽는다. 전부 "왜 그렇게 했는지"와 "어떤 함정을 밟았는지"가 핵심이다.

| 파일 | 내용 |
|---|---|
| `docs/engine.md` | 추천 로직 — 점수 체계, 토템, 낭비 판정, 타워 모드 |
| `docs/data.md` | 데이터 파일 구조, prydwenTags, 오버스펙 플래그 |
| `docs/ai.md` | AI 설명 생성, 캐시·레이트리밋·일일 상한 |
| `docs/i18n.md` | 다국어(A·B·C단계), 커뮤니티 번역, 용어집·이름 보호 |
| `docs/board.md` | 게시판 수정·삭제·비밀글 RLS |
| `docs/security.md` | 보안 사고 이력과 대응 |
| `docs/ops.md` | Supabase·Vercel·배포·환경변수 |
| `docs/pitfalls.md` | 개발 환경의 함정 |
| `docs/weekly-research.md` | 주간 데이터 조사 예약 작업 |
| `docs/open-items.md` | 열려 있는 작업·판단 대기 항목 |
| `docs/claude-code.md` | 클로드 코드 이관·설정 방법 |

과거 인수인계 문서 `HANDOFF.md`는 위 파일들로 분리했다(2026-08-12). 원문은 `git show ff9997e:HANDOFF.md`.

경로별 규칙은 `.claude/rules/`에 있다 — 해당 파일을 건드릴 때만 자동으로 들어온다.
검사 실행은 `/verify` 또는 `npm run verify`.

---

## 작업 규칙

### git

- **`git add -A` 금지.** `git status`에 41개가 뜨지만 30개는 **줄바꿈(CRLF/LF) 차이**이며
  clone 시점부터 있던 상태다. 반드시 **파일을 이름으로 지정해 `git add`** 할 것.
- 커밋 메시지는 **무엇을 왜 고쳤는지**를 적는다. 특히 실측·역테스트 결과를 남긴다.
- `.env.local`에 Supabase 키와 `ANTHROPIC_API_KEY`가 있다. **절대 커밋하지 말 것.**

### 비밀키

- 환경변수 이름에 **`NEXT_PUBLIC_` 을 붙이면 브라우저 번들에 실려 공개된다.**
  `SUPABASE_SERVICE_ROLE_KEY`에는 절대 붙이지 않는다.
- Vercel 환경변수는 **추가 후 재배포해야** 런타임에 반영된다.

### 데이터 갱신

- **A(1차 출처에서 그대로 옮긴 값)만 자동 반영, B(추론·해석)는 제안만.**
- 캐릭터를 추가하면 `id/title/name_kr/name_ja/class/burst/element/weapon/releaseDate/tiers/
  skills[3]`을 모두 채운다. `name_ja`가 없으면 `checkData`가 ERROR를 낸다.

---

## 사용자에 대해 알아둘 것

- **게임 지식이 깊다.** 크크마 로테이션, 프리카+민트 픽률, 오버스펙 규칙, 낡은 조합 목록 등
  여러 차례 데이터 오류를 게임 지식으로 잡아냈다. **지적은 대체로 맞으니 진지하게 검증할 것.**
- **근거를 중시한다.** "왜 이렇게 됐는지"를 자주 묻는다. 추측으로 답하지 말고 실제로
  재현·측정해서 답한다. 이 프로젝트의 거의 모든 결정이 실측 뒤에 내려졌다.
- **틀렸으면 인정하고 고치는 것을 선호한다.** 실제로 여러 번 판단이 틀렸고(크라운 제외 근거,
  마스트 토템 설명, 이미지 한쪽만 수정, 해외명 출처가 "없다"고 단정) 바로잡은 기록이 주석에 남아 있다.
- 응답은 **간결하게.** 불필요한 설명을 싫어한다.
- 기능을 늘리는 것보다 **틀린 정보를 없애는 쪽**을 택한다. 낡은 조합 목록을 고치지 않고
  지운 것이 그 예다(`lib/recommend.js` 주석 참고).
